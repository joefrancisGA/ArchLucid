import { RemediationFactoryClient } from "@/app/(operator)/governance/remediation-factory/RemediationFactoryClient";

/** SecureNow remediation factory — ranked queue and executive metrics. */
export default function SecureNowRemediationFactoryPage() {
  return <RemediationFactoryClient />;
}
