import { DemoEntryRedirect } from "@/components/cto-demo/DemoEntryRedirect";

/** Shareable demo entry — lands on step 1 of the CTO tour (#18). */
export default function DemoEntryPage() {
  return <DemoEntryRedirect />;
}
