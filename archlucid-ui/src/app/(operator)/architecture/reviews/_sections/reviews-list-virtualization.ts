export {
  OPERATOR_LIST_VIRTUALIZE_MIN_ROWS as REVIEWS_LIST_VIRTUALIZE_MIN_ROWS,
  shouldVirtualizeOperatorList as shouldVirtualizeReviewsList,
} from "@/lib/operator/operator-list-virtualization";

/** Estimated hub / runs-list table row height for virtualization (compact operator rows). */
export const REVIEWS_LIST_ROW_ESTIMATE_PX = 88;
