import { REPLAY_MODE_PLAIN_OPTIONS } from "@/lib/replay-display";

/** Matches ArchLucid.Persistence.Replay.ReplayMode — default to lightest mode. */
export const defaultReplayMode = REPLAY_MODE_PLAIN_OPTIONS[0]!.mode;
