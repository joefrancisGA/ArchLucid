import { SsoWizardPageClient } from "./_sections/SsoWizardPageClient";

/** Admin-only five-step SSO configuration wizard (OIDC / SAML 2.0). */
export default function SsoWizardPage() {
  return <SsoWizardPageClient />;
}
