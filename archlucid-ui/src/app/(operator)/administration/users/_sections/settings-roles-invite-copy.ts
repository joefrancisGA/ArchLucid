/** LS-009 — admin invites bind to the inviter's current operator workspace scope. */
export const SETTINGS_ROLES_INVITE_WORKSPACE_SCOPE_HINT =
  "Invitations join your current workspace. Switch workspace in the header before sending if you need a different destination.";

export const SETTINGS_ROLES_INVITE_WORKSPACE_SCOPE_HINT_TEST_ID =
  "settings-roles-invite-workspace-scope-hint" as const;
