/**
 * Traffic workbook row ID for Alert rules Conditions (rules) tab.
 * Owner backlog shorthand: GLR (owner GOR renamed to avoid collision with template routing GOR / ALE).
 */
export const ALERT_RULES_RULES_TAB_TRAFFIC_ROW_ID = "GLR";

/** Canonical path tracked on the GLR workbook row (explicit tab query; hub SAX is bare path). */
export const ALERT_RULES_RULES_TAB_TRAFFIC_PATH = "/governance/alert-rules?tab=rules";

/** Workbook Section column value Ã¢â‚¬â€ query-tab deep link on alert-rules hub. */
export const ALERT_RULES_RULES_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for GLR - documents Evidence chrome inherited from SAX hub on Conditions tab.
 * ASCII-only for Windows console note scripts.
 */
export const ALERT_RULES_RULES_TAB_TRAFFIC_NOTE =
  "Alert rules Conditions/rules tab (Tab surface) - inherits SAX hub Evidence chrome (PageContextualHelpButton + Category-1 registry on /governance/alert-rules; Sources follow-up chrome removed (TB-2092) Sources + claim-discipline when not on Notifications). AlertRulesContentDeferred condition editor. Sibling SAX = hub; ALE/GOR = routing; GOC = composite; GOS = simulation. Alert configuration - not a signed-record Sources trail.tab-surface ceiling below parent hub Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner GOR renamed to GLR to avoid collision with template routing GOR. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
