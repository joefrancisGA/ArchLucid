/**
 * Traffic workbook row ID for OIDC identity-provider settings.
 * Owner backlog shorthand: AOI (template formerly SOI on legacy settings path).
 */
export const IDENTITY_PROVIDERS_OIDC_TRAFFIC_ROW_ID = "AOI";

/** Canonical path tracked on the AOI workbook row. */
export const IDENTITY_PROVIDERS_OIDC_TRAFFIC_PATH = "/administration/identity-providers/oidc";

/** Workbook Section column value (template catalog). */
export const IDENTITY_PROVIDERS_OIDC_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for AOI - documents Evidence chrome on OIDC settings.
 * ASCII-only for Windows console note scripts.
 */
export const IDENTITY_PROVIDERS_OIDC_TRAFFIC_NOTE =
  "OIDC identity provider (Settings/Admin) - IdentityProvidersOidcPageView with PageContextualHelpButton (shared header; topic map enterprise-onboarding; Category-1 registry), workspace Sources + claim-discipline orientation strip, discovery / authority / audience status. Sibling AID = identity-providers hub; ADO = role-mapping; HEX = enterprise-onboarding help. Access configuration - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-06) - settings access-hub hard-caps higher Evidence.";
