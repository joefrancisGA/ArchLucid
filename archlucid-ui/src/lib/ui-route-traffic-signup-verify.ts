/**
 * Traffic workbook row ID for Signup verify.
 * Owner backlog shorthand: SVX.
 */
export const SIGNUP_VERIFY_TRAFFIC_ROW_ID = "SVX";

/** Canonical path tracked on the SVX workbook row. */
export const SIGNUP_VERIFY_TRAFFIC_PATH = "/signup/verify";

/** Workbook Section column value. */
export const SIGNUP_VERIFY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for SVX — documents Evidence chrome on Signup verify.
 */
export const SIGNUP_VERIFY_TRAFFIC_NOTE =
  "Signup verify (Marketing) - SignupVerifyClient email-verification handoff with SignupVerifyEvidenceOrientationStrip (evaluation Sources + claim-discipline: evaluation access only; not diligence / CPA SOC 2 / third-party pen-test). Not an operator PageContextualHelp surface. Sibling SIG = /signup. Does not imply CPA SOC 2 or third-party pen-test publication. Score 40/100 (2026-08-04) — marketing evaluation-access page hard-caps higher Evidence.";
