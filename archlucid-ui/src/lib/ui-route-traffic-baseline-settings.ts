/**
 * Traffic workbook row ID for operator Baseline settings.
 * Owner backlog shorthand: ADA (template formerly SBX on legacy settings/baseline path).
 */
export const BASELINE_SETTINGS_TRAFFIC_ROW_ID = "ADA";

/** Canonical path tracked on the ADA workbook row. */
export const BASELINE_SETTINGS_TRAFFIC_PATH = "/administration/baseline";

/** Workbook Section column value (template catalog). */
export const BASELINE_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADA - documents Evidence chrome on Baseline settings.
 * ASCII-only for Windows console note scripts.
 */
export const BASELINE_SETTINGS_TRAFFIC_NOTE =
  "Baseline settings (Settings/Admin) - BaselineSettingsClient with PageContextualHelpButton (topic map pilot-roi-model; Category-1 registry), workspace Sources + claim-discipline orientation strip, ROI measurement anchors + save/clear. Sibling PI = pilot-roi-model help; SCX = architecture-scorecard; ABI = billing. Measurement anchors - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-05) - settings measurement-hub hard-caps higher Evidence.";
