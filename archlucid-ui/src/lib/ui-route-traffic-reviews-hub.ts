/**
 * Traffic workbook row ID for Architecture reviews list.
 * Owner backlog shorthand: RE.
 */
export const REVIEWS_HUB_TRAFFIC_ROW_ID = "RE";

/** Canonical path tracked on the RE workbook row. */
export const REVIEWS_HUB_TRAFFIC_PATH = "/architecture/reviews";

/** Workbook Section column value. */
export const REVIEWS_HUB_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for RE - documents Evidence chrome on Architecture reviews.
 * ASCII-only for Windows console note scripts.
 */
export const REVIEWS_HUB_TRAFFIC_NOTE =
 "Architecture reviews hub (Core review) - RunsPageView with ReviewsHubHeaderActions PageContextualHelpButton (topic map review-packages; Category-1 registry), summary/inventory/drafts. Sibling RRE = review detail; RNX = start review; RRF = finding detail. Not a signed-record Sources trail by itself. Score 58/100 (2026-08-08) - reviews inventory launcher hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
