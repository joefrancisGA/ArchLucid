/** Legacy bookmark path — App Router shim forwards to canonical onboarding (TB-1801). */
export const LEGACY_ONBOARDING_START_PATH = "/onboarding/start";

/** Canonical in-product onboarding hub tracked on traffic row ONB. */
export const CANONICAL_ONBOARDING_PATH = "/onboarding";
