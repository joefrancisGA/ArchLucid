/**
 * Traffic workbook row ID for Improvement planning.
 * Owner backlog shorthand: PLA.
 */
export const PLANNING_TRAFFIC_ROW_ID = "PLA";

/** Canonical path tracked on the PLA workbook row. */
export const PLANNING_TRAFFIC_PATH = "/insights/planning";

/** Workbook Section column value. */
export const PLANNING_TRAFFIC_SECTION = "Planning";

/**
 * Owner workbook Notes for PLA — documents Evidence chrome on Improvement planning.
 */
export const PLANNING_TRAFFIC_NOTE =
  "Improvement planning - PlanningPageView with PageContextualHelp (topic map how-it-works / Improvement planning; Category-1 registry), Sources follow-up strip + claim-discipline callout (derived themes/plans, not diligence trail), themes/plans tables, export readiness. Not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication.";
