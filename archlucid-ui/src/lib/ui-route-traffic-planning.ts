/**
 * Traffic workbook row ID for Improvement planning.
 * Owner backlog shorthand: PLA.
 */
export const PLANNING_TRAFFIC_ROW_ID = "PLA";

/** Canonical path tracked on the PLA workbook row. */
export const PLANNING_TRAFFIC_PATH = "/insights/planning";

/** Workbook Section column value. */
export const PLANNING_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for PLA — documents Evidence chrome on Improvement planning.
 */
export const PLANNING_TRAFFIC_NOTE =
  "Improvement planning - PlanningPageView with PageContextualHelp (topic map getting-started / Improvement planning; Category-1 registry), Sources follow-up strip + dismissible claim-discipline callout (derived themes/plans, not diligence trail), empty-path composition (CTA + maturity/outcome orientation; hides zero KPIs/export until plans exist), priority-score explain, themes/plans tables, export readiness. Not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 54/100 (2026-08-03) — empty-path honesty improved; aggregate planning launcher still hard-caps higher Evidence.";
