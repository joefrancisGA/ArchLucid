/**
 * Traffic workbook row ID for Improvement planning.
 * Owner backlog shorthand: PLA.
 */
export const PLANNING_TRAFFIC_ROW_ID = "PLA";

/** Canonical path tracked on the PLA workbook row. */
export const PLANNING_TRAFFIC_PATH = "/insights/improvement-planning";

/** Workbook Section column value. */
export const PLANNING_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for PLA - documents Evidence chrome on Improvement planning.
 * ASCII-only for Windows console note scripts.
 */
export const PLANNING_TRAFFIC_NOTE =
  "Improvement planning (Marketing) - PlanningPageView with PageContextualHelp (Learn more omitted - no planning specialty; TB-2050; Category-1 registry), Sources follow-up chrome removed (TB-2092) + PlanningClaimDisciplineCallout (derived themes/plans, not diligence trail), empty-path composition (CTA + maturity/outcome orientation; hides zero KPIs/export until plans exist), priority-score explain, themes/plans tables, export readiness. Not a signed-record Sources trail. Score 68/100 (2026-08-08) - aggregate planning launcher at SCX Insights Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
