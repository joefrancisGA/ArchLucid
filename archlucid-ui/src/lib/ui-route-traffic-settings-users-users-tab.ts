import { SETTINGS_USERS_USERS_TAB_PATH } from "@/lib/settings-admin-route-paths";

/**
 * Traffic workbook row ID for Users and roles Users tab.
 * Owner backlog shorthand: SSU.
 */
export const SETTINGS_USERS_USERS_TAB_TRAFFIC_ROW_ID = "SSU";

/** Canonical path tracked on the SSU workbook row. */
export const SETTINGS_USERS_USERS_TAB_TRAFFIC_PATH = SETTINGS_USERS_USERS_TAB_PATH;

/** Workbook Section column value - query-tab on Users and roles hub. */
export const SETTINGS_USERS_USERS_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for SSU - documents Evidence chrome inherited from AUX hub on Users tab.
 * ASCII-only for Windows console note scripts.
 */
export const SETTINGS_USERS_USERS_TAB_TRAFFIC_NOTE =
 "Users and roles Users tab (Tab surface) - inherits AUX hub Evidence chrome (SettingsRolesPageView PageContextualHelpButton + Category-1 registry on /administration/users; Sources follow-up chrome removed (TB-2092) above tabs). SettingsRolesPageView syncs ?tab=users via router.replace for SSU deep-links; mounts invite + pending invitations. Sibling SER = roles; AUX = hub.tab-surface ceiling below parent hub Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
