export const MANIFEST_DETAIL_DECISIONS_OPEN_PARAM = "manifestDetailDecisionsOpen";

export function parseManifestDetailDecisionsOpenFromSearch(raw: string | null | undefined): boolean {
  if (raw === null || raw === undefined) {
    return false;
  }

  const trimmed = raw.trim().toLowerCase();

  return trimmed === "1" || trimmed === "true";
}

export function manifestDetailDecisionsDisclosureHrefFromSearch(
  currentSearch: string,
  open: boolean,
  pathname: string,
): string {
  const params = new URLSearchParams(currentSearch);

  if (!open) {
    params.delete(MANIFEST_DETAIL_DECISIONS_OPEN_PARAM);
  } else {
    params.set(MANIFEST_DETAIL_DECISIONS_OPEN_PARAM, "1");
  }

  const nextQuery = params.toString();

  return nextQuery.length === 0 ? pathname : `${pathname}?${nextQuery}`;
}
