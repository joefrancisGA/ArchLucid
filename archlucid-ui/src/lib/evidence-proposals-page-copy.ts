/** Operator-facing copy for `/internal/evidence-proposals`. */

/** Shown when the proposals list request fails without a usable ProblemDetails body, or the fetch rejects. */
export const EVIDENCE_PROPOSALS_LOAD_UNEXPECTED_ERROR =
  "Could not load evidence proposals. Refresh the page or try again later.";

/** Shown when promote fails without a usable ProblemDetails body, or the fetch rejects. */
export const EVIDENCE_PROPOSALS_PROMOTE_UNEXPECTED_ERROR =
  "Could not promote this proposal. Refresh the list and try again.";
