/**
 * Traffic workbook row ID for Policy packs hub.
 * Owner backlog shorthand: GPP.
 */
export const POLICY_PACKS_HUB_TRAFFIC_ROW_ID = "GPP";

/** Canonical path tracked on the GPP workbook row. */
export const POLICY_PACKS_HUB_TRAFFIC_PATH = "/governance/policy-packs";

/** Workbook Section column value. */
export const POLICY_PACKS_HUB_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GPP - documents Evidence chrome on policy packs hub.
 * ASCII-only for Windows console note scripts.
 */
export const POLICY_PACKS_HUB_TRAFFIC_NOTE =
  "Policy packs hub (Alerts/gov) - PolicyPacksPageClient with PolicyPacksPageHeader PageContextualHelpButton (topic map policy-packs; Category-1 registry), PolicyPacksEvidenceOrientationStrip (workspace Sources + claim-discipline: pack library only), catalog/my-packs tabs and authoring. Sibling GPI = pack detail; GRS = standards-and-rules. Not a signed-record Sources trail. Score 48/100 (2026-08-08) - governance library hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
