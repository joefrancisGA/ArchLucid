/**
 * Traffic workbook row ID for get-started marketing page.
 * Owner backlog shorthand: GXX.
 */
export const GET_STARTED_TRAFFIC_ROW_ID = "GXX";

/** Canonical path tracked on the GXX workbook row. */
export const GET_STARTED_TRAFFIC_PATH = "/get-started";

/** Workbook Section column value (template catalog). */
export const GET_STARTED_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for GXX — documents Evidence chrome on get-started.
 */
export const GET_STARTED_TRAFFIC_NOTE =
  "Get started (Marketing) - GetStartedPageClient with GetStartedEvidenceOrientationStrip (evaluation Sources + claim-discipline: first-run orientation only; not diligence / CPA SOC 2 / third-party pen-test). Not an operator PageContextualHelp surface. Legacy /quick-start retired toward this path. Does not imply CPA SOC 2 or third-party pen-test publication. Score 40/100 (2026-08-04) — marketing first-run page hard-caps higher Evidence.";
