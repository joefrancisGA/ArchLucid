/**
 * Traffic workbook row ID for Administration / Settings hub.
 * Owner backlog shorthand: SET.
 */
export const ADMINISTRATION_HUB_TRAFFIC_ROW_ID = "SET";

/** Canonical path tracked on the SET workbook row. */
export const ADMINISTRATION_HUB_TRAFFIC_PATH = "/administration";

/** Workbook Section column value. */
export const ADMINISTRATION_HUB_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for SET - documents Evidence chrome on Settings hub.
 * ASCII-only for Windows console note scripts.
 */
export const ADMINISTRATION_HUB_TRAFFIC_NOTE =
  "Settings hub (Admin) - SettingsPageView with SettingsMasterOverviewHeader PageContextualHelpButton (topic map configuration-reference; Category-1 registry), section search/cards. Sibling ADY = system-health; AUX = users; ATE = tenant. Not a signed-record Sources trail by itself. Score 68/100 (2026-08-08) - configuration launcher at ADY Admin Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
