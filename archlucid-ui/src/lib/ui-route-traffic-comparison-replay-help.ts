/**
 * Traffic workbook row ID for Compare and replay help.
 * Owner backlog shorthand: CO.
 */
export const COMPARISON_REPLAY_HELP_TRAFFIC_ROW_ID = "CO";

/** Canonical path tracked on the CO workbook row. */
export const COMPARISON_REPLAY_HELP_TRAFFIC_PATH = "/help/comparison-replay";

/** Workbook Section column value - in-app help topic, not marketing. */
export const COMPARISON_REPLAY_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for CO - documents Evidence chrome on comparison-replay help.
 * ASCII-only for Windows console note scripts.
 */
export const COMPARISON_REPLAY_HELP_TRAFFIC_NOTE =
 "Compare and replay help (Help topic) - HelpTopicMarkdownView with PageContextualHelpButton (topic map comparison-replay; Category-1 registry), curated COMPARISON_REPLAY_OPERATOR_GUIDE.md. Sibling CXX = Compare two reviews; REP = Validate review; HRX = repeat-review-loop; HE. = catch-all residual. Not bare HelpTopicMarkdownView without orientation. Score 58/100 (2026-08-07) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
