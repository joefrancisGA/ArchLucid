/**
 * Shared buyer-polished copy — import here instead of scattering literals across pages.
 *
 * Copy now lives under `./buyer-copy/`, one module per product surface. This module stays as the
 * established import path for existing callers; new callers may import `@/lib/buyer-copy` directly.
 */

export * from ".././buyer-copy";
