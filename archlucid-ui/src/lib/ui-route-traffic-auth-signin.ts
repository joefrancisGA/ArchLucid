/**
 * Traffic workbook row ID for Sign in.
 * Owner backlog shorthand: ASI.
 */
export const AUTH_SIGNIN_TRAFFIC_ROW_ID = "ASI";

/** Canonical path tracked on the ASI workbook row. */
export const AUTH_SIGNIN_TRAFFIC_PATH = "/auth/signin";

/** Workbook Section column value. */
export const AUTH_SIGNIN_TRAFFIC_SECTION = "Auth";

/**
 * Owner workbook Notes for ASI - documents Evidence chrome on Sign in.
 * ASCII-only for Windows console note scripts.
 */
export const AUTH_SIGNIN_TRAFFIC_NOTE =
  "Sign in (Auth) - SignInClient/SignInFlowClient with SignInEvidenceOrientationStrip (public Sources + claim-discipline: authentication gate only; not diligence / CPA SOC 2 / third-party pen-test), AuthFlowShell email/SSO/code steps. Not an operator PageContextualHelp surface. Sibling ACB = callback; AUB = bootstrap; ASU = session-expired. Does not imply CPA SOC 2 or third-party pen-test publication. Score 54/100 (2026-08-08) - auth gate hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
