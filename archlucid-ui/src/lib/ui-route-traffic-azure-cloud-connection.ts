/**
 * Traffic workbook row ID for Azure cloud connection settings.
 * Owner backlog shorthand: IAZ.
 */
export const AZURE_CLOUD_CONNECTION_TRAFFIC_ROW_ID = "IAZ";

/** Canonical path tracked on the IAZ workbook row. */
export const AZURE_CLOUD_CONNECTION_TRAFFIC_PATH = "/integrations/cloud-connections/azure";

/** Workbook Section column value (template catalog). */
export const AZURE_CLOUD_CONNECTION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for IAZ - documents Evidence chrome on Azure cloud connection.
 * ASCII-only for Windows console note scripts.
 */
export const AZURE_CLOUD_CONNECTION_TRAFFIC_NOTE =
  "Azure cloud connection (Integrations) - AzureCloudConnectionDetailClient with CloudConnectionsProviderHeader PageContextualHelpButton (topic map cloud-connections-azure; Category-1 registry), CloudProviderConnectionEvidenceOrientationStrip (workspace Sources + claim-discipline), security preflight + service-principal federation + Tier2 wizard. Sibling SCE = cloud-connections hub; INC = AWS; IGC = GCP; HC = connect-azure help. Read-only connector config — not a signed-record Sources trail. Score 48/100 (2026-08-08) - integration-config hub hard-caps higher Evidence.";
