/**

 * Stable operator surfaces (help, redirects with static loaders) may prerender.

 * Data routes should use `operator-data-route-policy.ts` instead.

 */

export const dynamic = "force-static";

