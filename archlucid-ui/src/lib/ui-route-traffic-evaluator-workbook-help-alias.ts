/**
 * Traffic workbook row ID for the legacy evaluator-workbook help bookmark.
 * Owner backlog shorthand: HEE.
 */
export const EVALUATOR_WORKBOOK_HELP_ALIAS_TRAFFIC_ROW_ID = "HEE";

/** Legacy alias path tracked on the HEE workbook row. */
export const EVALUATOR_WORKBOOK_HELP_ALIAS_TRAFFIC_PATH = "/help/evaluator-workbook";

/** Canonical path-chooser help path that HEE resolves to (workbook row HPX). */
export const EVALUATOR_WORKBOOK_HELP_ALIAS_CANONICAL_PATH = "/help/path-chooser";

/** Workbook Section column value - deprecated help alias, not a separate topic. */
export const EVALUATOR_WORKBOOK_HELP_ALIAS_TRAFFIC_SECTION = "Help alias";

/**
 * Owner workbook Notes for HEE - documents the legacy slug alias bookmark.
 * ASCII-only for Windows console note scripts.
 */
export const EVALUATOR_WORKBOOK_HELP_ALIAS_TRAFFIC_NOTE =
 "Deprecated evaluator-workbook help alias (Help alias) - slug alias evaluator-workbook -> path-chooser; renders HelpPathChooserGuideView Evidence chrome (Sources strip + claim-discipline + PageContextualHelp + Category-1 on alias path). Canon HPX = /help/path-chooser. Pass/hold body lives in BUYER_ORIENTATION_ONE_SCREEN.md. Does not imply CPA SOC 2 or third-party pen-test publication. Score 52/100 (2026-08-05) - help-topic orientation hard-caps higher Evidence (alias inherits HPX). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
