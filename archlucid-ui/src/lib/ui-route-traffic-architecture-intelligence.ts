import { ARCHITECTURE_INTELLIGENCE_PATH } from "@/lib/architecture-intelligence-route";

/**
 * Traffic workbook row ID for Architecture intelligence.
 * Owner backlog shorthand: AR2.
 */
export const ARCHITECTURE_INTELLIGENCE_TRAFFIC_ROW_ID = "AIN";

/** Canonical path tracked on the AIN workbook row. */
export const ARCHITECTURE_INTELLIGENCE_TRAFFIC_PATH = ARCHITECTURE_INTELLIGENCE_PATH;

/** Workbook Section column value — core review workflow surface, not pre-login marketing. */
export const ARCHITECTURE_INTELLIGENCE_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for AIN/AR2 - documents Evidence chrome on Architecture intelligence.
 * ASCII-only for Windows console note scripts.
 */
export const ARCHITECTURE_INTELLIGENCE_TRAFFIC_NOTE =
  "Architecture intelligence (Core review) - ArchitectureIntelligencePageClient with PageContextualHelpButton (topic map evidence-trail; Category-1 registry), workspace Sources + claim-discipline orientation strip, closed-loop reasoning / golden harness / publish-to-findings. Sibling RNX = start review; EV = evidence-trail help; HFX = findings help. Reasoning operator surface - not a signed-record Sources trail. Score 50/100 (2026-08-06) - closed-loop reasoning hub hard-caps higher Evidence.";
