/**
 * Shared sourcing sentence for ArchLucid tenant ID and managed identity object ID
 * used by GCP Workload Identity Federation setup (help + integrations).
 */
export const GCP_FEDERATION_IDENTIFIER_SOURCING =
  "Obtain the current tenant ID and managed identity object ID from Assurance status or the in-product security review when values are environment-specific.";
