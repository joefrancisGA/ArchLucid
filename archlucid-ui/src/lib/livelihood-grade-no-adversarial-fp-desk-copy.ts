/** LN-006 — adversarial hypothesis lane is named; rows are not dropped by default. */
export const LIVELIHOOD_GRADE_NO_ADVERSARIAL_FP_DESK_LINE =
  "Adversarial hypothesis findings are noisy by design — false positives are tracked, not silently removed." as const;
