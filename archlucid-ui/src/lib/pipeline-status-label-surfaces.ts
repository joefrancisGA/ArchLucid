/**
 * Buyer pipeline display labels scanned by {@link ./pipeline-status-label-guard.test.ts} (TB-651).
 */
export const PIPELINE_STATUS_BUYER_LABEL_BANNED_PATTERNS = [
  "ready to seal",
  "in flight",
  "in pipeline",
  "execution pipeline",
  "golden manifest",
] as const;
