/**
 * Traffic workbook row ID for Teams integration.
 * Owner backlog shorthand: ITX.
 */
export const TEAMS_INTEGRATION_TRAFFIC_ROW_ID = "ITX";

/** Canonical path tracked on the ITX workbook row. */
export const TEAMS_INTEGRATION_TRAFFIC_PATH = "/integrations/teams";

/** Workbook Section column value. */
export const TEAMS_INTEGRATION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for ITX - documents Evidence chrome on Teams integration.
 * ASCII-only for Windows console note scripts.
 */
export const TEAMS_INTEGRATION_TRAFFIC_NOTE =
  "Teams integration (Integrations) - TeamsNotificationsIntegrationPageView with PageContextualHelpButton (topic map alerts; Category-1 registry), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), connector form + notification selector. Notification routing config hub - not a signed-record Sources trail. Sibling ISN = Slack; SAX/GOR = alert rules. Score 68/100 (2026-08-08) - integration-config hub at SCE Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
