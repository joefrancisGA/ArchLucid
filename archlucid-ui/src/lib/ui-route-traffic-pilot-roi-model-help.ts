/**
 * Traffic workbook row ID for Pilot ROI model help.
 * Owner backlog shorthand: PI.
 */
export const PILOT_ROI_MODEL_HELP_TRAFFIC_ROW_ID = "PI";

/** Canonical path tracked on the PI workbook row. */
export const PILOT_ROI_MODEL_HELP_TRAFFIC_PATH = "/help/pilot-roi-model";

/** Workbook Section column value - in-app help topic, not marketing. */
export const PILOT_ROI_MODEL_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for PI - documents Evidence chrome on pilot-roi-model help.
 * ASCII-only for Windows console note scripts.
 */
export const PILOT_ROI_MODEL_HELP_TRAFFIC_NOTE =
  "Pilot ROI model help (Help topic) - HelpTopicMarkdownView with PageContextualHelpButton (topic map pilot-roi-model; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, curated PILOT_ROI_MODEL markdown. Sibling SCX = architecture-scorecard; SPP = pilot-outcomes; SPE = executive-summary; ADA = baseline settings. Score 58/100 (2026-08-05) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
