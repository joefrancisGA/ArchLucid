/**
 * Traffic workbook row ID for Admin diagnostics help.
 * Owner backlog shorthand: HAE.
 */
export const ADMIN_DIAGNOSTICS_HELP_TRAFFIC_ROW_ID = "HAE";

/** Canonical path tracked on the HAE workbook row. */
export const ADMIN_DIAGNOSTICS_HELP_TRAFFIC_PATH = "/help/admin-diagnostics";

/** Workbook Section column value - in-app help topic, not marketing. */
export const ADMIN_DIAGNOSTICS_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HAE - documents Evidence chrome on admin-diagnostics help.
 * ASCII-only for Windows console note scripts.
 */
export const ADMIN_DIAGNOSTICS_HELP_TRAFFIC_NOTE =
 "Admin diagnostics help (Help topic) - HelpAdminDiagnosticsGuideView with System health primary CTA / Workspace Overview secondary, action-panel Sources links (ADMIN_DIAGNOSTICS_HELP_SOURCES), claim-discipline callout, registry provenance (lastReviewed), prepared OPERATOR_ADMIN_DIAGNOSTICS.md. PageContextualHelpButton self-suppresses on /help/* paths (not rendered on this surface). Sibling HDX = eng troubleshooting; HCX = cli-usage; ADY = system-health; HE. = catch-all residual. Not bare HelpTopicMarkdownView. Score 58/100 (2026-08-07) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
