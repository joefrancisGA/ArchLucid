/**
 * Removed traffic workbook row ID for the retired `/example-roi-bulletin` scoring row.
 * Do not reintroduce — the marketing page is gone; EXA must not return.
 */
export const REMOVED_EXAMPLE_ROI_BULLETIN_TRAFFIC_ROW_ID = "EXA";

/** Retired path — not a live App Router page or next.config redirect. */
export const RETIRED_EXAMPLE_ROI_BULLETIN_TRAFFIC_PATH = "/example-roi-bulletin";
