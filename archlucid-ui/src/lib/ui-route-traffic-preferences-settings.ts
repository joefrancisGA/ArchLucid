import { SETTINGS_PREFERENCES_PATH } from "@/lib/settings-admin-route-paths";

/**
 * Traffic workbook row ID for Preferences settings.
 * Owner backlog shorthand: ADR (template formerly SEP on legacy settings/preferences path).
 */
export const PREFERENCES_SETTINGS_TRAFFIC_ROW_ID = "ADR";

/** Canonical path tracked on the ADR workbook row. */
export const PREFERENCES_SETTINGS_TRAFFIC_PATH = SETTINGS_PREFERENCES_PATH;

/** Workbook Section column value (template catalog). */
export const PREFERENCES_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADR - documents Evidence chrome on Preferences.
 * ASCII-only for Windows console note scripts.
 */
export const PREFERENCES_SETTINGS_TRAFFIC_NOTE =
 "Preferences (Settings) - PreferencesSettingsPageView with PageContextualHelpButton (topic map getting-started; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, appearance / theme. Sibling ADS = account-security; HGX = getting-started help. Personal account settings - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-05) - settings personal-hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
