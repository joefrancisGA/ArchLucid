/**
 * Traffic workbook row ID for ITSM connectors (admin).
 * Owner backlog shorthand: AII.
 */
export const ADMIN_ITSM_CONNECTORS_TRAFFIC_ROW_ID = "AII";

/** Canonical path tracked on the AII workbook row. */
export const ADMIN_ITSM_CONNECTORS_TRAFFIC_PATH = "/internal/integrations/itsm";

/** Workbook Section column value. */
export const ADMIN_ITSM_CONNECTORS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for AII - documents Evidence chrome on ITSM connectors.
 * ASCII-only for Windows console note scripts.
 */
export const ADMIN_ITSM_CONNECTORS_TRAFFIC_NOTE =
  "ITSM connectors (Admin) - AdminItsmConnectorsPageClient with PageContextualHelpButton (topic map integration-readiness; Category-1 registry), health probes + wizard. Sibling IJX/ISX = buyer Jira/ServiceNow; IIO = OAuth callback. Not a signed-record Sources trail. Score 78/100 (2026-08-08) - admin connector hub hard-caps short of diligence packing. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
