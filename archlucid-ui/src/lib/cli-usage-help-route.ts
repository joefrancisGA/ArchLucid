/** Canonical CLI usage runbook path — authority-gated help topic (registry slug `cli-usage`). */
export const CLI_USAGE_HELP_PATH = "/help/cli-usage" as const;

/** Retired internal duplicate — permanently redirects to {@link CLI_USAGE_HELP_PATH}. */
export const RETIRED_CLI_USAGE_INTERNAL_PATH = "/internal/cli-usage" as const;
