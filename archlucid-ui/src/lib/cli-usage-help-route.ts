/** Canonical CLI usage internal runbook path (registry slug `cli-usage`). */
export const CLI_USAGE_HELP_PATH = "/internal/cli-usage" as const;

/** Retired help bookmark — redirects to {@link CLI_USAGE_HELP_PATH}. */
export const RETIRED_CLI_USAGE_HELP_PATH = "/help/cli-usage" as const;
