/**
 * Traffic workbook row ID for Projects recycle bin.
 * Owner backlog shorthand was ARE (collides with template executive-dashboard);
 * template/catalog ID is STR.
 */
export const PROJECTS_RECYCLE_BIN_TRAFFIC_ROW_ID = "STR";

/** Canonical path tracked on the STR workbook row. */
export const PROJECTS_RECYCLE_BIN_TRAFFIC_PATH = "/administration/tenant/recycle-bin";

/** Workbook Section column value (template catalog). */
export const PROJECTS_RECYCLE_BIN_TRAFFIC_SECTION = "Settings";

/**
 * Owner workbook Notes for STR - documents Evidence chrome on Projects recycle bin.
 * ASCII-only for Windows console note scripts.
 */
export const PROJECTS_RECYCLE_BIN_TRAFFIC_NOTE =
  "Projects recycle bin (Settings/Admin) - ProjectsRecycleBinPage with PageContextualHelpButton (topic map scope; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, soft-deleted project restore. Sibling ATE = tenant settings; ARA = architectures list. Access configuration - not a signed-record Sources trail. Score 50/100 (2026-08-06) - settings access-hub hard-caps higher Evidence.";
