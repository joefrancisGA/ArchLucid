/**
 * Traffic workbook row ID for Prior manifest retrieval help.
 * Owner backlog shorthand: HPR.
 */
export const PRIOR_MANIFEST_RETRIEVAL_HELP_TRAFFIC_ROW_ID = "HPR";

/** Canonical path tracked on the HPR workbook row. */
export const PRIOR_MANIFEST_RETRIEVAL_HELP_TRAFFIC_PATH = "/help/prior-manifest-retrieval";

/** Workbook Section column value - in-app help topic. */
export const PRIOR_MANIFEST_RETRIEVAL_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HPR - documents Evidence chrome on Prior manifest retrieval help.
 * ASCII-only for Windows console note scripts.
 */
export const PRIOR_MANIFEST_RETRIEVAL_HELP_TRAFFIC_NOTE =
  "Prior manifest retrieval help (Help topic) - HelpTopicMarkdownView with PageContextualHelp +, curated PRIOR_MANIFEST_RETRIEVAL_GUIDE.md (TB-1733 host-config strip). Sibling ISE = search; INS = Ask; SI = signed records. Not bare catch-all help chrome. Score 58/100 (2026-08-08) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
