import { SETTINGS_SECURITY_TRUST_PATH } from "@/lib/settings-admin-route-paths";

/**
 * Traffic workbook row ID for operator Security & trust settings.
 * Owner backlog shorthand: WSX.
 */
export const SETTINGS_SECURITY_TRUST_TRAFFIC_ROW_ID = "WSX";

/** Canonical path tracked on the WSX workbook row. */
export const SETTINGS_SECURITY_TRUST_TRAFFIC_PATH = SETTINGS_SECURITY_TRUST_PATH;

/** Workbook Section column value (template catalog). */
export const SETTINGS_SECURITY_TRUST_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for WSX Ã¢â‚¬â€ documents Evidence chrome on settings security-trust.
 */
export const SETTINGS_SECURITY_TRUST_TRAFFIC_NOTE =
  "Security & trust (Settings) - OperatorSecurityTrustPageView with PageContextualHelpButton (topic map security-trust; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, procurement materials + NDA/roadmap honesty. Sibling SEC = /security-trust hub; HSE = /help/security-trust; TXX = /trust. Operator orientation Ã¢â‚¬â€ not a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
