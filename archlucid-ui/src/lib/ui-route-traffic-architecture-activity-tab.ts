/**
 * Traffic workbook row ID for create-home Activity archTab.
 * Owner backlog shorthand: REA.
 */
export const ARCHITECTURE_ACTIVITY_TAB_TRAFFIC_ROW_ID = "REA";

/** Canonical path tracked on the REA workbook row. */
export const ARCHITECTURE_ACTIVITY_TAB_TRAFFIC_PATH =
 "/architecture/reviews/[runId]?archTab=activity" as const;

/** Workbook Section column value â€” create-home tab surface. */
export const ARCHITECTURE_ACTIVITY_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for REA â€” create-home Activity archTab Evidence chrome.
 * ASCII-only for Windows console note scripts.
 */
export const ARCHITECTURE_ACTIVITY_TAB_TRAFFIC_NOTE =
 "Create-home Activity archTab (Tab surface) - mounts on ArchitectureCreatedWorkspace when fromGeneration+create-architecture and no signed review record; ArchitectureCreatedWorkspace syncs ?archTab=activity. above progress tracker / technology baseline / outcome cards (TB-1846 sibling band). Twin committed surface is reviewTab=activity on ReviewDetailWorkspace (RRE hub chrome). Sibling REG = governance; RED = diagram; REE = evidence; REF = findings; REO = overview; REC = clarifications. Assessment progress only - not a signed-record Sources trail.tab-surface ceiling below parent hub Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
