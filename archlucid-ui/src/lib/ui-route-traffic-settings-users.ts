import { SETTINGS_USERS_PATH } from "@/lib/settings-admin-route-paths";

/**
 * Traffic workbook row ID for Users and roles settings.
 * Owner backlog shorthand: AUX.
 */
export const SETTINGS_USERS_TRAFFIC_ROW_ID = "AUX";

/** Canonical path tracked on the AUX workbook row. */
export const SETTINGS_USERS_TRAFFIC_PATH = SETTINGS_USERS_PATH;

/** Workbook Section column value (template catalog). */
export const SETTINGS_USERS_TRAFFIC_SECTION = "Settings";

/**
 * Owner workbook Notes for AUX — documents Evidence chrome on Users and roles.
 */
export const SETTINGS_USERS_TRAFFIC_NOTE =
  "Users and roles (Settings/Admin) - SettingsRolesPageView with PageContextualHelpButton (topic map users-and-roles; Category-1 registry), workspace Sources + claim-discipline orientation strip, Users/Roles/API-keys tabs, invite + matrix. Access configuration — not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-03) — access-hub hard-caps higher Evidence.";
