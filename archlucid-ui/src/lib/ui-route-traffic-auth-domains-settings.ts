/**
 * Traffic workbook row ID for Sign-in domains (auth-domains) settings.
 * Owner backlog shorthand: ADU (template formerly SEU on legacy settings/auth-domains path).
 */
export const AUTH_DOMAINS_SETTINGS_TRAFFIC_ROW_ID = "ADU";

/** Canonical path tracked on the ADU workbook row. */
export const AUTH_DOMAINS_SETTINGS_TRAFFIC_PATH = "/administration/auth-domains";

/** Workbook Section column value (template catalog). */
export const AUTH_DOMAINS_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADU - documents Evidence chrome on Sign-in domains.
 * ASCII-only for Windows console note scripts.
 */
export const AUTH_DOMAINS_SETTINGS_TRAFFIC_NOTE =
  "Sign-in domains (Settings/Admin) - AuthDomainsPageClient with PageContextualHelpButton (topic map enterprise-onboarding; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, domain verify / routing test / SSO enforcement. Sibling AID = identity-providers; ADS = account-security; HEX = enterprise-onboarding help. Access configuration - not a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
