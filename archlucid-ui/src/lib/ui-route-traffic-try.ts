/**
 * Traffic workbook row ID for try marketing page.
 * Owner backlog shorthand: TRY.
 */
export const TRY_TRAFFIC_ROW_ID = "TRY";

/** Canonical path tracked on the TRY workbook row. */
export const TRY_TRAFFIC_PATH = "/try";

/** Workbook Section column value (template catalog). */
export const TRY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for TRY — documents Evidence chrome on try.
 */
export const TRY_TRAFFIC_NOTE =
  "Try (Marketing) - TryPage with TryEvidenceOrientationStrip (evaluation Sources + claim-discipline: frictionless sample only; not diligence / CPA SOC 2 / third-party pen-test). Not an operator PageContextualHelp surface. Sibling GXX = /get-started; SRH = /showcase/[runId]. Does not imply CPA SOC 2 or third-party pen-test publication. Score 40/100 (2026-08-05) — marketing frictionless-try page hard-caps higher Evidence.";
