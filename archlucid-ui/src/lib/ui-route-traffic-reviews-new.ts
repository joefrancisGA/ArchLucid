/**
 * Traffic workbook row ID for Start review intake.
 * Owner backlog shorthand: RNX.
 */
export const REVIEWS_NEW_TRAFFIC_ROW_ID = "RNX";

/** Canonical path tracked on the RNX workbook row. */
export const REVIEWS_NEW_TRAFFIC_PATH = "/architecture/reviews/new";

/** Workbook Section column value. */
export const REVIEWS_NEW_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for RNX â€” documents Evidence chrome on Start review.
 */
export const REVIEWS_NEW_TRAFFIC_NOTE =
  "Start review intake (Core review) - ReviewsNewPageChrome with OperatorPageHeader PageContextualHelpButton (topic map evidence-intake / Start review; Category-1 registry), ReviewsNewSourcesStrip (workspace Sources + claim-discipline: intake launcher only), path switcher wizards. Sibling REQ/REN/ENE = path tabs; RE = reviews hub. Not a signed-record Sources trail by itself. Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-08) - intake launcher hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
