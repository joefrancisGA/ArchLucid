/** Distinct accessible name for accelerator pack start CTAs. */
export function buildAcceleratorPackStartAriaLabel(packLabel: string, buyerJob: string): string {
  return `Start with the ${packLabel} for ${buyerJob}`;
}
