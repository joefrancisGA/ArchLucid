/**
 * Traffic workbook row ID for operator Overview home (`/`).
 * Owner backlog shorthand: HOM.
 */
export const OPERATOR_HOME_TRAFFIC_ROW_ID = "HOM";

/** Canonical path tracked on the HOM workbook row. */
export const OPERATOR_HOME_TRAFFIC_PATH = "/";

/** Workbook Section column value — Core review launcher, not marketing. */
export const OPERATOR_HOME_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for HOM - documents Evidence chrome on Overview home.
 * ASCII-only for Windows console note scripts.
 */
export const OPERATOR_HOME_TRAFFIC_NOTE =
 "Operator Overview home (Core review) - OperatorHomePageView with remounted OperatorHomePageChrome (title/refresh/PageContextualHelpButton topic map first-architecture-review + Category-1 registry for `/`), phase-aware PilotCommandCenterCard (Do-this-next / dual-path / NBA; in-card help suppressed to avoid double icons), Recent reviews, ROI strip, sample explore, workspace context. TB-1667 HOM slice. Sibling RE = reviews; ARE = executive-dashboard; RNX = start review. Not a signed-record Sources trail by itself. Score 58/100 (2026-08-08) - Overview launcher hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
