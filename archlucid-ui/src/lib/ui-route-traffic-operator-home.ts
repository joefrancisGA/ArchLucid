/**
 * Traffic workbook row ID for operator Overview home (`/`).
 * Owner backlog shorthand: HOM.
 */
export const OPERATOR_HOME_TRAFFIC_ROW_ID = "HOM";

/** Canonical path tracked on the HOM workbook row. */
export const OPERATOR_HOME_TRAFFIC_PATH = "/";

/** Workbook Section column value — Core review launcher, not marketing. */
export const OPERATOR_HOME_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for HOM — documents the live Overview command-center surface.
 */
export const OPERATOR_HOME_TRAFFIC_NOTE =
  "Operator Overview home - OperatorHomePageView with remounted OperatorHomePageChrome (title/refresh/PageContextualHelp -> first-architecture-review + Category-1 registry for `/`), phase-aware PilotCommandCenterCard (Do-this-next / dual-path / NBA; in-card help suppressed to avoid double icons), Recent reviews, ROI strip, sample explore, workspace context. TB-1667 HOM slice. Not a diligence Sources trail.";
