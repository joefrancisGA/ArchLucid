/**
 * Traffic workbook row ID for welcome marketing page.
 * Owner backlog shorthand: WXX.
 */
export const WELCOME_TRAFFIC_ROW_ID = "WXX";

/** Canonical path tracked on the WXX workbook row. */
export const WELCOME_TRAFFIC_PATH = "/welcome";

/** Workbook Section column value (template catalog). */
export const WELCOME_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for WXX â€” documents Evidence chrome on welcome.
 */
export const WELCOME_TRAFFIC_NOTE =
 "Welcome (Marketing) - WelcomeMarketingPage with WelcomeEvidenceOrientationStrip (evaluation Sources + claim-discipline: marketing landing only). Not an operator PageContextualHelp surface. Sibling SEE = /see-it; GXX = /get-started; SEC = /security-trust. Score 40/100 (2026-08-05) â€” marketing welcome page hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
