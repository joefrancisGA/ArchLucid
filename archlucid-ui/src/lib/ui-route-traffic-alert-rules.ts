/**
 * Traffic workbook row ID for Alert rules hub (default Conditions tab).
 * Owner backlog shorthand: SAX.
 */
export const ALERT_RULES_TRAFFIC_ROW_ID = "SAX";

/** Canonical path tracked on the SAX workbook row. */
export const ALERT_RULES_TRAFFIC_PATH = "/governance/alert-rules";

/** Workbook Section column value (owner catalog). */
export const ALERT_RULES_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for SAX - documents Evidence chrome on Alert rules hub.
 * ASCII-only for Windows console note scripts.
 */
export const ALERT_RULES_TRAFFIC_NOTE =
 "Alert rules hub (Alerts/gov) - AlertRulesHubClient with PageContextualHelpButton (topic map alerts; Category-1 registry), Learn more / claim-discipline orientation strip on non-routing tabs (Sources follow-up removed TB-2092; Notifications/routing keeps ALE sibling chrome), Conditions/Notifications/Advanced/Test tabs. Alert configuration - not a signed-record Sources trail. Sibling ALE/GOR = routing tab; GOA = low-hit hub duplicate in template. Score 48/100 (2026-08-08) - alert-config hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
