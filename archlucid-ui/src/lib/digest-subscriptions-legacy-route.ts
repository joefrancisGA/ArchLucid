import {
  DIGESTS_SUBSCRIPTIONS_TAB_PATH,
  LEGACY_DIGEST_SUBSCRIPTIONS_PATH,
} from "@/lib/digests-route-paths";

/** Retired subscriptions bookmark (IXX / TB-1495). */
export const RETIRED_DIGEST_SUBSCRIPTIONS_PATH = LEGACY_DIGEST_SUBSCRIPTIONS_PATH;

/** Canonical Digests Subscriptions tab (traffic row AIS). */
export const CANONICAL_DIGEST_SUBSCRIPTIONS_PATH = DIGESTS_SUBSCRIPTIONS_TAB_PATH;
