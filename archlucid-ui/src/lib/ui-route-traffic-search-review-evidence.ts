/**
 * Traffic workbook row ID for Search review evidence.
 * Owner backlog shorthand: SXX.
 */
export const SEARCH_REVIEW_EVIDENCE_TRAFFIC_ROW_ID = "SXX";

/** Canonical path tracked on the SXX workbook row. */
export const SEARCH_REVIEW_EVIDENCE_TRAFFIC_PATH = "/insights/search-review-evidence";

/** Workbook Section column value. */
export const SEARCH_REVIEW_EVIDENCE_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for SXX - documents Evidence chrome on search review evidence.
 * ASCII-only for Windows console note scripts.
 */
export const SEARCH_REVIEW_EVIDENCE_TRAFFIC_NOTE =
  "Search review evidence (Insights) - SearchPageView with PageContextualHelpButton (topic map; Category-1 registry), optional SearchReviewEvidenceCiteStrip when scoped to a review. Not a signed-record Sources trail by itself. Score 68/100 (2026-08-08) - retrieval launcher at SCX Insights Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
