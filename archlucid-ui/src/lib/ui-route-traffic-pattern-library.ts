/**
 * Traffic workbook row ID for Pattern library hub.
 * Owner backlog shorthand: INP (template formerly PXX).
 */
export const PATTERN_LIBRARY_TRAFFIC_ROW_ID = "INP";

/** Canonical path tracked on the INP workbook row. */
export const PATTERN_LIBRARY_TRAFFIC_PATH = "/insights/patterns";

/** Workbook Section column value (catalog; insights operator surface). */
export const PATTERN_LIBRARY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for INP - documents Evidence chrome on Pattern library.
 * ASCII-only for Windows console note scripts.
 */
export const PATTERN_LIBRARY_TRAFFIC_NOTE =
  "Pattern library hub (Insights) - PatternLibraryPageClient with OperatorPageHeader PageContextualHelpButton (Category-1 registry; topic map label-only Learn more omitted TB-2050), provenance badge + filters + pattern cards. Sibling INA = /insights/patterns/[patternKey]. Anonymized/thresholded catalog Ã¢â‚¬â€ not a signed-record Sources trail.marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
