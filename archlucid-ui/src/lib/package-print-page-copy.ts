export const PACKAGE_PRINT_PRIMARY_CONTENT_ID = "package-print-primary-content" as const;

export const PACKAGE_PRINT_FIRST_VIEWPORT_ID = "package-print-first-viewport" as const;

export const PACKAGE_PRINT_FIRST_VIEWPORT_TEST_ID = PACKAGE_PRINT_FIRST_VIEWPORT_ID;

export const PACKAGE_PRINT_SKIP_TARGET_ID = PACKAGE_PRINT_FIRST_VIEWPORT_ID;

export const PACKAGE_PRINT_SKIP_LINK_LABEL = "Skip to print summary" as const;

export const PACKAGE_PRINT_PAGE_SUBTITLE_BUYER =
  "Print-friendly review summary — use Save as PDF for handoffs; this is not a finalized export artifact." as const;

export const PACKAGE_PRINT_CLAIM_HEADING = "Print orientation only" as const;
