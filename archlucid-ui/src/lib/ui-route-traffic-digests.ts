/**
 * Traffic workbook row ID for Architecture digests.
 * Owner backlog shorthand: DI.
 */
export const DIGESTS_TRAFFIC_ROW_ID = "DI";

import { DIGESTS_HUB_PATH } from "@/lib/digests-route-paths";

/** Canonical path tracked on the DI workbook row. */
export const DIGESTS_TRAFFIC_PATH = DIGESTS_HUB_PATH;

/** Workbook Section column value. */
export const DIGESTS_TRAFFIC_SECTION = "Digests";

/**
 * Owner workbook Notes for DI — documents Evidence chrome on Architecture digests.
 */
export const DIGESTS_TRAFFIC_NOTE =
  "Architecture digests - DigestsHubClient with DigestsPageHeader PageContextualHelp (topic map digests / Architecture digests; Category-1 registry + Schedule deep links), tab bar directly under the header, one primary header action (next unresolved setup step, or Preview latest generated digest once configured), Sources follow-up strip below the tabs with no claim-boundary band (owner decision 2026-08-05), weekly health banner status-only on Browse so the Browse setup checklist owns setup guidance. Learn more → /help/digests (HDG). Not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 68/100 (2026-08-05) after TB-1480 / TB-1501-TB-1505 / TB-2049 - hub launcher hard-caps higher Evidence.";
