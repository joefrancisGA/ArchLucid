/**
 * Traffic workbook row ID for Architecture digests.
 * Owner backlog shorthand: DI.
 */
export const DIGESTS_TRAFFIC_ROW_ID = "DI";

/** Canonical path tracked on the DI workbook row. */
export const DIGESTS_TRAFFIC_PATH = "/digests";

/** Workbook Section column value. */
export const DIGESTS_TRAFFIC_SECTION = "Digests";

/**
 * Owner workbook Notes for DI — documents Evidence chrome on Architecture digests.
 */
export const DIGESTS_TRAFFIC_NOTE =
  "Architecture digests - DigestsHubClient with DigestsPageHeader PageContextualHelp (topic map how-it-works / Architecture digests; Category-1 registry), Sources follow-up strip + claim-discipline callout (scheduled summaries, not diligence trail), browse/subscriptions/schedule tabs, weekly health banner. Not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication.";
