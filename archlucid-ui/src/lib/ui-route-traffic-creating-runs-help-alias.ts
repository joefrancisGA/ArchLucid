/**
 * Traffic workbook row ID for legacy `/help/creating-runs` alias.
 * Owner backlog shorthand: HER (TB-1644).
 */
export const CREATING_RUNS_HELP_ALIAS_TRAFFIC_ROW_ID = "HER";

/** Canonical path tracked on the HER workbook row. */
export const CREATING_RUNS_HELP_ALIAS_TRAFFIC_PATH = "/help/creating-runs";

/** Workbook Section column value - retired slug alias under Help. */
export const CREATING_RUNS_HELP_ALIAS_TRAFFIC_SECTION = "Help alias";

/**
 * Owner workbook Notes for HER - documents Evidence chrome inherited via HelpReviewGuideView.
 * ASCII-only for Windows console note scripts.
 */
export const CREATING_RUNS_HELP_ALIAS_TRAFFIC_NOTE =
 "Deprecated creating-runs help alias (Help alias) - slug alias creating-runs -> review-guide; renders HelpReviewGuideView Evidence chrome (Sources strip + claim-discipline + PageContextualHelp + Category-1 on alias path). Canon HR = /help/review-guide. Body lives in REVIEW_GUIDE.md (TB-1644). Sibling HET = starting-reviews alias. Score 52/100 (2026-08-07) - help-topic orientation hard-caps higher Evidence (alias inherits HR). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
