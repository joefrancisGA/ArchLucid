/**
 * Traffic workbook row ID for the canonical review workspace detail.
 * Owner backlog shorthand: RRE.
 */
export const REVIEW_WORKSPACE_TRAFFIC_ROW_ID = "RRE";

/** Canonical path tracked on the RRE workbook row. */
export const REVIEW_WORKSPACE_TRAFFIC_PATH = "/architecture/reviews/[runId]";

/** Workbook Section column value. */
export const REVIEW_WORKSPACE_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for RRE - documents Evidence chrome on review workspace detail.
 * ASCII-only for Windows console note scripts.
 */
export const REVIEW_WORKSPACE_TRAFFIC_NOTE =
  "Review workspace detail (Core review) - RunDetailPageView with PageContextualHelpButton (topic map review-packages; Category-1 registry), decision snapshot, findings-first reviewTab queue (toolbar + disposition rows before narrative), buyer-safe Evidence cited labels with technical-id disclosure, decision explainability confidence reconciliation, tab-aware primary CTAs, and HelpPageSituationRegistrar publishing review-approval-blocked so the Help drawer leads with Resolve findings that block approval. Absorbs former SRN hit share from retired legacy /snapshot/[runId] bookmark (hard-retired; use /architecture/reviews/{runId}?readOnly=1 leave-behind). Sibling RE = reviews list; RRF = finding detail; ERU = evidence-trace. Not a full signed-record diligence package alone. Score 76/100 (2026-08-09) - al-ui-rate P0 findings-tab IA pass plus al-ui-rate P0 Help drawer pass (non-modal contextual drawer, situation-aware Do this now, flat divider rows, collapsed onboarding group, honest support footer label, keyboard-hint contrast); hard-caps higher Evidence without signed-record diligence Sources trail.";
