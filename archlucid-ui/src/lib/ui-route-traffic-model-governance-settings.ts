/**
 * Traffic workbook row ID for AI and model governance settings.
 * Owner backlog shorthand: AMO (template formerly SEM on legacy settings path).
 */
export const MODEL_GOVERNANCE_SETTINGS_TRAFFIC_ROW_ID = "AMO";

/** Canonical path tracked on the AMO workbook row. */
export const MODEL_GOVERNANCE_SETTINGS_TRAFFIC_PATH = "/administration/model-governance";

/** Workbook Section column value (template catalog). */
export const MODEL_GOVERNANCE_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for AMO - documents Evidence chrome on model governance.
 * ASCII-only for Windows console note scripts.
 */
export const MODEL_GOVERNANCE_SETTINGS_TRAFFIC_NOTE =
  "AI and model governance (Settings/Admin) - ModelGovernanceSettingsPage with PageContextualHelpButton (topic map getting-started; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, execution profile / alias registry. Sibling ADI = ai-usage; ABI = billing; HBX = billing-and-plans help. Model profile controls - not a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
