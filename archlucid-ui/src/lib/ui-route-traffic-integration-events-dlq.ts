/**
 * Traffic workbook row ID for integration event dead letters.
 * Owner backlog shorthand: OID.
 */
export const INTEGRATION_EVENTS_DLQ_TRAFFIC_ROW_ID = "OID";

/** Canonical path tracked on the OID workbook row. */
export const INTEGRATION_EVENTS_DLQ_TRAFFIC_PATH = "/internal/integration-events/dlq";

/** Workbook Section column value (template catalog). */
export const INTEGRATION_EVENTS_DLQ_TRAFFIC_SECTION = "Advisory";

/**
 * Owner workbook Notes for OID Ã¢â‚¬â€ documents Evidence chrome on the DLQ page.
 */
export const INTEGRATION_EVENTS_DLQ_TRAFFIC_NOTE =
  "Integration event dead letters (Advisory/Admin ops) - IntegrationEventsDlqPageClient with PageContextualHelpButton (topic map integration-readiness; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, cross-tenant DLQ list + retry/suppress. Internal Operations triage Ã¢â‚¬â€ not a signed-record Sources trail. Sibling IWX = webhooks; IJX = Jira; ADY = system-health.admin KPI/config ceiling below ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
