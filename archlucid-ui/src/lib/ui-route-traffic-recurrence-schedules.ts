/**
 * Traffic workbook row ID for Recurrence schedules.
 * Owner backlog shorthand: GRX.
 */
export const RECURRENCE_SCHEDULES_TRAFFIC_ROW_ID = "GRX";

/** Canonical path tracked on the GRX workbook row. */
export const RECURRENCE_SCHEDULES_TRAFFIC_PATH = "/governance/recurrence-schedules";

/** Workbook Section column value. */
export const RECURRENCE_SCHEDULES_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GRX - documents Evidence chrome on Recurrence schedules.
 * ASCII-only for Windows console note scripts.
 */
export const RECURRENCE_SCHEDULES_TRAFFIC_NOTE =
  "Recurrence schedules (Alerts/gov) - RecurrenceSchedulesClient with PageContextualHelpButton (topic map governance-approval; Category-1 registry), create/activate table. Sibling GFN = findings; ADV = advisory scans. Not a signed-record Sources trail. Score 68/100 (2026-08-08) - schedule config at GFN Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
