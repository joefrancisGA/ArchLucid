import { AI_USAGE_SETTINGS_PATH } from "@/lib/ai-usage-nav-paths";

/**
 * Traffic workbook row ID for AI usage and cost settings.
 * Owner backlog shorthand: ADI (template formerly SC on legacy settings/ai-usage path).
 */
export const AI_USAGE_SETTINGS_TRAFFIC_ROW_ID = "ADI";

/** Canonical path tracked on the ADI workbook row. */
export const AI_USAGE_SETTINGS_TRAFFIC_PATH = AI_USAGE_SETTINGS_PATH;

/** Workbook Section column value (template catalog). */
export const AI_USAGE_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADI - documents Evidence chrome on AI usage and cost.
 * ASCII-only for Windows console note scripts.
 */
export const AI_USAGE_SETTINGS_TRAFFIC_NOTE =
  "AI usage and cost (Settings/Admin) - CostReportingSettingsPageView with PageContextualHelpButton (topic map billing-and-plans; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, KPIs / budget / daily usage / activity. Sibling ABI = billing; HBX = billing-and-plans help. Legacy AAX /admin/ai-usage-cost is redirect-only; canonical ADI path is /administration/ai-usage. Estimated spend controls - not invoice-accurate or a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
