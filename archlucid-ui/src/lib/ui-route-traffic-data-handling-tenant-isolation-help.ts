import { DATA_HANDLING_TENANT_ISOLATION_HELP_PATH } from "@/lib/data-handling-tenant-isolation-help-route";

/**
 * Traffic workbook row ID for Data handling and tenant isolation help.
 * Owner backlog shorthand: HDA.
 */
export const DATA_HANDLING_TENANT_ISOLATION_HELP_TRAFFIC_ROW_ID = "HDA";

/** Canonical path tracked on the HDA workbook row. */
export const DATA_HANDLING_TENANT_ISOLATION_HELP_TRAFFIC_PATH = DATA_HANDLING_TENANT_ISOLATION_HELP_PATH;

/** Workbook Section column value — in-app help topic, not marketing. */
export const DATA_HANDLING_TENANT_ISOLATION_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HDA — documents the specialty guide surface.
 */
export const DATA_HANDLING_TENANT_ISOLATION_HELP_TRAFFIC_NOTE =
  "Specialty data-handling + tenant isolation guide — HelpDataHandlingTenantIsolationGuideView with Trust Center / security-trust / audit primary CTAs, Sources diligence strip (trust, audit-trail, data-handling overview, subprocessors, DPA, procurement), residency honesty callout, PageContextualHelp, and prepared markdown (three-layer isolation, TB-1659 leakage strip, TB-1653 honesty). Not bare HelpTopicMarkdownView. Does not imply CPA SOC 2 or third-party pen-test publication.";
