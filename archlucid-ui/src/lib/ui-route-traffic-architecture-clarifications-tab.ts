/**
 * Traffic workbook row ID for create-home Clarifications archTab.
 * Owner backlog shorthand: REC.
 */
export const ARCHITECTURE_CLARIFICATIONS_TAB_TRAFFIC_ROW_ID = "REC";

/** Canonical path tracked on the REC workbook row. */
export const ARCHITECTURE_CLARIFICATIONS_TAB_TRAFFIC_PATH =
 "/architecture/reviews/[runId]?archTab=clarifications" as const;

/** Workbook Section column value â€” create-home tab surface. */
export const ARCHITECTURE_CLARIFICATIONS_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for REC â€” create-home Clarifications archTab Evidence chrome.
 * ASCII-only for Windows console note scripts.
 */
export const ARCHITECTURE_CLARIFICATIONS_TAB_TRAFFIC_NOTE =
 "Create-home Clarifications archTab (Tab surface) - mounts on ArchitectureCreatedWorkspace when fromGeneration+create-architecture and no signed manifest; ArchitectureCreatedWorkspace syncs ?archTab=clarifications. above ArchitectureCreatedClarificationsPanel (missing items + open questions). Sibling REA = activity; REG = governance; RED = diagram; REE = evidence; REF = findings; REO = overview. Clarifications only - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-08) - create-home path-tab hard-caps higher Evidence (below RRE hub). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
