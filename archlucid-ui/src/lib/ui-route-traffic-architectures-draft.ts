/**
 * Traffic workbook row ID for Architecture draft detail.
 * Owner backlog shorthand: ARR (template ARA remains legacy `/architectures/[architectureId]`).
 */
export const ARCHITECTURES_DRAFT_TRAFFIC_ROW_ID = "ARR";

/** Canonical path tracked on the ARR workbook row. */
export const ARCHITECTURES_DRAFT_TRAFFIC_PATH = "/architecture/architectures/[architectureId]";

/** Workbook Section column value (template catalog). */
export const ARCHITECTURES_DRAFT_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for ARR - documents Evidence chrome on architecture draft detail.
 * ASCII-only for Windows console note scripts.
 */
export const ARCHITECTURES_DRAFT_TRAFFIC_NOTE =
  "Architecture draft detail (Core review) - ArchitectureDraftPage with, ArchitectureDraftWorkspace PageContextualHelpButton (topic map getting-started; Category-1 via pathIsArchitectureDraftDetail), draft form + Start a review handoff. Sibling ARA = architectures list; ANE = create bootstrap; RNX = start review. Legacy `/architectures/[architectureId]` bookmarks map here. Saving a draft does not start a review; not a signed-record Sources trail. Score 68/100 (2026-08-06) - drafting workspace Evidence chrome (below ANE create-bootstrap 58). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail. operator surface at GFN/RE Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail.";
