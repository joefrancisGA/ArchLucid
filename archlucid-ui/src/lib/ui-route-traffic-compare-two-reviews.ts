/**
 * Traffic workbook row ID for Compare two reviews.
 * Owner backlog shorthand: CXX.
 */
export const COMPARE_TWO_REVIEWS_TRAFFIC_ROW_ID = "CXX";

/** Canonical path tracked on the CXX workbook row. */
export const COMPARE_TWO_REVIEWS_TRAFFIC_PATH = "/insights/compare-two-reviews";

/** Workbook Section column value. */
export const COMPARE_TWO_REVIEWS_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for CXX - documents Evidence chrome on Compare two reviews.
 * ASCII-only for Windows console note scripts.
 */
export const COMPARE_TWO_REVIEWS_TRAFFIC_NOTE =
  "Compare two reviews (Insights) - CompareForm with OperatorPageHeader PageContextualHelpButton (topic map comparison-replay; Category-1 registry), CompareEvidenceOrientationStrip (workspace Sources + claim-discipline), pair Cite Sources after Compare (ComparePairEvidenceCiteStrip), run pickers/results. Formerly /compare (retired; no redirect). Not a signed-record Sources trail by itself. Does not imply CPA SOC 2 or third-party pen-test publication. Score 52/100 (2026-08-08) - comparison launcher hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
