/**
 * Traffic workbook row ID for Pilot feedback / product learning.
 * Owner backlog shorthand: PRC.
 */
export const PRODUCT_LEARNING_TRAFFIC_ROW_ID = "PRC";

/** Canonical path tracked on the PRC workbook row. */
export const PRODUCT_LEARNING_TRAFFIC_PATH = "/internal/product-learning";

/** Workbook Section column value (template catalog). */
export const PRODUCT_LEARNING_TRAFFIC_SECTION = "Onboarding";

/**
 * Owner workbook Notes for PRC — documents Evidence chrome on Pilot feedback.
 */
export const PRODUCT_LEARNING_TRAFFIC_NOTE =
  "Pilot feedback (Onboarding) - ProductLearningPageView at /internal/product-learning with PageContextualHelpButton (topic map pilot-feedback; Category-1 registry), workspace Sources + claim-discipline orientation strip, time-range dashboard, planning bridge, exports. Legacy /product-learning hard-retired (404). Not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-04) — internal ops feedback hub hard-caps higher Evidence (below PLA planning).";
