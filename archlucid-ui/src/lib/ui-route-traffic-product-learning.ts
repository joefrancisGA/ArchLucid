/**
 * Traffic workbook row ID for Pilot feedback / product learning.
 * Owner backlog shorthand: INR.
 */
export const PRODUCT_LEARNING_TRAFFIC_ROW_ID = "INR";

/** Canonical path tracked on the INR workbook row. */
export const PRODUCT_LEARNING_TRAFFIC_PATH = "/internal/product-learning";

/** Workbook Section column value (template catalog). */
export const PRODUCT_LEARNING_TRAFFIC_SECTION = "Onboarding";

/**
 * Owner workbook Notes for INR — documents Evidence chrome on Pilot feedback.
 */
export const PRODUCT_LEARNING_TRAFFIC_NOTE =
  "Pilot feedback (Onboarding) - ProductLearningPageView at /internal/product-learning with PageContextualHelpButton (topic map pilot-feedback; Category-1 registry), time-range dashboard, planning bridge, exports. Legacy /product-learning hard-retired (404). Sibling HPE = pilot-feedback help; INE = recommendation-learning; PLA = improvement planning. Not a signed-record Sources trail. Score 50/100 (2026-08-08) - internal ops feedback hub hard-caps higher Evidence (below PLA planning).";
