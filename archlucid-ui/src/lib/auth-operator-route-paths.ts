/** Canonical operator sign-in surface (traffic row ASI). */
export const AUTH_SIGNIN_PATH = "/auth/signin" as const;

/** Idle-timeout handoff after session expiry. */
export const AUTH_SESSION_EXPIRED_PATH = "/auth/session-expired" as const;
