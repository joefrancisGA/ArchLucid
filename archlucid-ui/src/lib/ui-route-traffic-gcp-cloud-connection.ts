/**
 * Traffic workbook row ID for GCP cloud connection settings.
 * Owner backlog shorthand: IGC.
 */
export const GCP_CLOUD_CONNECTION_TRAFFIC_ROW_ID = "IGC";

/** Canonical path tracked on the IGC workbook row. */
export const GCP_CLOUD_CONNECTION_TRAFFIC_PATH = "/integrations/cloud-connections/gcp";

/** Workbook Section column value (template catalog). */
export const GCP_CLOUD_CONNECTION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for IGC - documents Evidence chrome on GCP cloud connection.
 * ASCII-only for Windows console note scripts.
 */
export const GCP_CLOUD_CONNECTION_TRAFFIC_NOTE =
 "GCP cloud connection (Integrations) - GcpCloudConnectionDetailClient with CloudConnectionsProviderHeader PageContextualHelpButton (topic map cloud-connections-gcp; Category-1 registry), security preflight + Workload Identity Federation + connection details. Sibling SCE = cloud-connections hub; INC = AWS; IAZ = Azure; HGC = connect-gcp help. Read-only connector config â€” not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-08) - integration-config hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
