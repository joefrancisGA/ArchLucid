/**
 * Traffic workbook row ID for invitation accept handoff.
 * Owner backlog shorthand: AUI.
 */
export const AUTH_INVITE_TRAFFIC_ROW_ID = "AUI";

/** Canonical path tracked on the AUI workbook row. */
export const AUTH_INVITE_TRAFFIC_PATH = "/auth/invite";

/** Workbook Section column value (template catalog). */
export const AUTH_INVITE_TRAFFIC_SECTION = "Auth";

/**
 * Owner workbook Notes for AUI - documents Evidence chrome on auth invite.
 * ASCII-only for Windows console note scripts.
 */
export const AUTH_INVITE_TRAFFIC_NOTE =
  "Auth invite (Auth) - InvitationAcceptPageClient with. Token validation + Continue to sign in. Not an operator PageContextualHelp surface (pre-sign-in handoff). Sibling AUB = bootstrap; ASU = session-expired; ASI = signin; SRI = invite-reviewer admin.auth-gate ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
