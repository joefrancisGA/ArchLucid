/**
 * Traffic workbook row ID for Standards & rules.
 * Owner backlog shorthand: GRS.
 */
export const STANDARDS_RULES_TRAFFIC_ROW_ID = "GRS";

/** Canonical path tracked on the GRS workbook row. */
export const STANDARDS_RULES_TRAFFIC_PATH = "/governance/standards-and-rules";

/** Workbook Section column value. */
export const STANDARDS_RULES_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GRS - documents Evidence chrome on Standards & rules.
 * ASCII-only for Windows console note scripts.
 */
export const STANDARDS_RULES_TRAFFIC_NOTE =
  "Standards & rules (Alerts/gov) - GovernanceResolutionPageClient with PageContextualHelpButton (topic map policy-packs; Category-1 registry), StandardsRulesEvidenceOrientationStrip (workspace Sources + claim-discipline: effective rules only), applied-rule table and resolution diagnostics. Sibling GPP = policy-packs hub; GPI = pack detail. Not a signed-record Sources trail. Score 48/100 (2026-08-08) - governance resolution hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
