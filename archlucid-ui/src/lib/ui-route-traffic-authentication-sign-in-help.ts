/**
 * Traffic workbook row ID for Authentication and sign-in help.
 * Owner backlog shorthand: HEA.
 */
export const AUTHENTICATION_SIGN_IN_HELP_TRAFFIC_ROW_ID = "HEA";

/** Canonical path tracked on the HEA workbook row. */
export const AUTHENTICATION_SIGN_IN_HELP_TRAFFIC_PATH = "/help/authentication-sign-in";

/** Workbook Section column value - in-app help topic, not marketing. */
export const AUTHENTICATION_SIGN_IN_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HEA - documents Evidence chrome on authentication-sign-in help.
 * ASCII-only for Windows console note scripts.
 */
export const AUTHENTICATION_SIGN_IN_HELP_TRAFFIC_NOTE =
 "Authentication sign-in help (Help topic) - HelpTopicMarkdownView with PageContextualHelpButton (topic map authentication-sign-in; Category-1 registry), curated authentication/sign-in markdown. Sibling HOE = users-and-roles; ADS = account-security; CON = configuration-reference; HE. = catch-all residual. Not bare HelpTopicMarkdownView without orientation. Score 58/100 (2026-08-07) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
