import { SIGNED_RECORDS_LIST_PATH } from "@/lib/signed-records-paths";

/**
 * Traffic workbook row ID for sealed review records list hub.
 * Owner backlog shorthand: SI (template formerly MA).
 */
export const SIGNED_RECORDS_LIST_TRAFFIC_ROW_ID = "SI";

/** Canonical path tracked on the SI workbook row. */
export const SIGNED_RECORDS_LIST_TRAFFIC_PATH = SIGNED_RECORDS_LIST_PATH;

/** Workbook Section column value. */
export const SIGNED_RECORDS_LIST_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for SI - documents Evidence chrome on signed-records list.
 * ASCII-only for Windows console note scripts.
 */
export const SIGNED_RECORDS_LIST_TRAFFIC_NOTE =
  "Sealed review records list (Alerts/gov) - SignedRecordsListClient with OperatorPageHeader PageContextualHelpButton (topic map review-packages; Category-1 registry on /governance/sealed-records), finalized-run table with Open review / Open sealed record. Sibling MMX = package detail; GAR = artifact preview. Template MA renamed to SI to match owner.signed-records list at MMX Evidence band; hard-caps higher Evidence without sealed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a sealed-record diligence Sources trail.";
