/**
 * Traffic workbook row ID for Risk exceptions.
 * Owner backlog shorthand: GRO.
 */
export const RISK_EXCEPTIONS_TRAFFIC_ROW_ID = "GRO";

/** Canonical path tracked on the GRO workbook row. */
export const RISK_EXCEPTIONS_TRAFFIC_PATH = "/governance/exceptions";

/** Workbook Section column value. */
export const RISK_EXCEPTIONS_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GRO - documents Evidence chrome on Risk exceptions.
 * ASCII-only for Windows console note scripts.
 */
export const RISK_EXCEPTIONS_TRAFFIC_NOTE =
  "Risk exceptions (Alerts/gov) - RiskExceptionsClient with PageContextualHelpButton (topic map findings; Category-1 registry), renew/revoke table. Sibling GFN = findings; GDO = decision register. Not a signed-record Sources trail by itself. Score 68/100 (2026-08-08) - waiver register at GFN Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
