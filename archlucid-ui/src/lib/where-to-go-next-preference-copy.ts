/** Anchor for the follow-up strip visibility card (distinct from page-level `#where-to-go-next` link strips). */
export const PREFERENCES_FOLLOW_UP_LINK_STRIPS_ANCHOR_ID = "follow-up-link-strips" as const;

/** Personal preferences card — visibility for follow-up link strips (page-level strips keep the shorter "Where to go next" heading). */
export const PREFERENCES_WHERE_TO_GO_NEXT_HEADING = "Follow-up link strips";

export const PREFERENCES_WHERE_TO_GO_NEXT_LEAD =
  "Show or hide Where to go next follow-up link strips on operator pages.";

export const PREFERENCES_WHERE_TO_GO_NEXT_TOGGLE_LABEL = "Show follow-up link strips";
