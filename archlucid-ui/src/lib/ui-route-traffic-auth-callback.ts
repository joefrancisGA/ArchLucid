/**
 * Traffic workbook row ID for Auth callback.
 * Owner backlog shorthand: ACB.
 */
export const AUTH_CALLBACK_TRAFFIC_ROW_ID = "ACB";

/** Canonical path tracked on the ACB workbook row. */
export const AUTH_CALLBACK_TRAFFIC_PATH = "/auth/callback";

/** Workbook Section column value. */
export const AUTH_CALLBACK_TRAFFIC_SECTION = "Auth";

/**
 * Owner workbook Notes for ACB - documents Evidence chrome on Auth callback.
 * ASCII-only for Windows console note scripts.
 */
export const AUTH_CALLBACK_TRAFFIC_NOTE =
  "Auth callback (Auth) - CallbackClient with AuthFlowShell loading/failure panels, PKCE token exchange. Not an operator PageContextualHelp surface. Sibling ASI = signin; AUB = bootstrap. Score 58/100 (2026-08-08) - OAuth callback ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
