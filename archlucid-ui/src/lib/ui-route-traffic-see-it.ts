/**
 * Traffic workbook row ID for see-it marketing page.
 * Owner backlog shorthand: SEE.
 */
export const SEE_IT_TRAFFIC_ROW_ID = "SEE";

/** Canonical path tracked on the SEE workbook row. */
export const SEE_IT_TRAFFIC_PATH = "/see-it";

/** Workbook Section column value (template catalog). */
export const SEE_IT_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for SEE Ã¢â‚¬â€ documents Evidence chrome on see-it.
 */
export const SEE_IT_TRAFFIC_NOTE =
  "See it (Marketing) - SeeItMarketingPage with SeeItEvidenceOrientationStrip (evaluation Sources + claim-discipline: fabricated sample proof only). Not an operator PageContextualHelp surface. Sibling LXX = /live-demo; DPX = /demo/preview; SRH = /showcase/[runId].marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
