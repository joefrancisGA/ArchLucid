/**
 * Traffic workbook row ID for Digests Schedule tab.
 * Owner backlog shorthand: DIS (canonical). Do not reintroduce retired SEX/EEX.
 */
export const DIGESTS_SCHEDULE_TRAFFIC_ROW_ID = "DIS";

/** Canonical path tracked on the DIS workbook row. */
export const DIGESTS_SCHEDULE_TRAFFIC_PATH = "/digests?tab=schedule";

/** Workbook Section column value (template catalog). */
export const DIGESTS_SCHEDULE_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for DIS — documents Evidence chrome on Digests Schedule.
 */
export const DIGESTS_SCHEDULE_TRAFFIC_NOTE =
  "Digests Schedule tab (Tab surface) - ExecDigestScheduleContent with hub DigestsPageHeader PageContextualHelp (topic map getting-started / Architecture digests; Category-1 registry), DigestsScheduleEvidenceOrientationStrip (Sources + claim-discipline) when ?tab=schedule. Delivery cadence only — not diligence Sources. Sibling DI = hub; DIB = browse; DIX = subscriptions. Hit share folded from a retired mistaken schedule bookmark row (legacy settings exec-digest IDs remain removed — do not reintroduce). Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-03) — schedule-config tab hard-caps higher Evidence.";
