/**
 * Traffic workbook row ID for Digests Schedule tab.
 * Owner backlog shorthand: DIS.
 */
export const DIGESTS_SCHEDULE_TRAFFIC_ROW_ID = "DIS";

import { DIGESTS_SCHEDULE_TAB_PATH } from "@/lib/digests-route-paths";

export const DIGESTS_SCHEDULE_TRAFFIC_PATH = DIGESTS_SCHEDULE_TAB_PATH;

export const DIGESTS_SCHEDULE_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for DIS — Digests Schedule tab Evidence chrome.
 */
export const DIGESTS_SCHEDULE_TRAFFIC_NOTE =
  "Digests Schedule tab (Tab surface) - ExecDigestScheduleContent with hub DigestsPageHeader PageContextualHelp (topic map digests / Architecture digests; Category-1 registry + Schedule deep links), DigestsScheduleEvidenceOrientationStrip (Sources only - no claim-boundary band, owner decision 2026-08-05) when ?tab=schedule. Delivery cadence only — not diligence Sources. Sibling DI = hub; DIB = browse; DIX = subscriptions; HDG = Digests help. Hit share folded from a retired mistaken schedule bookmark row (legacy settings exec-digest IDs remain removed — do not reintroduce). Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-03) — schedule-config tab hard-caps higher Evidence.";
