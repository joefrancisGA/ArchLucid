/**
 * Traffic workbook row ID for approval lineage.
 * Owner backlog shorthand: GAI.
 */
export const APPROVAL_LINEAGE_TRAFFIC_ROW_ID = "GAI";

/** Canonical path pattern tracked on the GAI workbook row. */
export const APPROVAL_LINEAGE_TRAFFIC_PATH = "/governance/approval-requests/[id]/lineage";

/** Workbook Section column value (owner catalog). */
export const APPROVAL_LINEAGE_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GAI - documents Evidence chrome on approval lineage.
 * ASCII-only for Windows console note scripts.
 */
export const APPROVAL_LINEAGE_TRAFFIC_NOTE =
  "Approval lineage (Alerts/gov) - GovernanceApprovalLineageDetailContent with PageContextualHelpButton (topic map governance-approval; Category-1 registry on /governance/approval-requests), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), approval status/risk, review + signed-record version links, findings/completeness. Governance linkage view - not a full diligence Sources package alone. Does not imply CPA SOC 2 or third-party pen-test publication. Score 55/100 (2026-08-08) - lineage linkage hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
