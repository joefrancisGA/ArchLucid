/**
 * Traffic workbook row ID for finding evidence-trace (canonical inspect surface).
 * Owner backlog shorthand: ERU.
 */
export const EVIDENCE_TRACE_TRAFFIC_ROW_ID = "ERU";

/** Canonical path tracked on the ERU workbook row. */
export const EVIDENCE_TRACE_TRAFFIC_PATH =
 "/architecture/reviews/[reviewId]/findings/[findingId]/evidence-trace";

/** Workbook Section column value. */
export const EVIDENCE_TRACE_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for ERU - documents Evidence chrome on finding evidence-trace.
 * ASCII-only for Windows console note scripts.
 */
export const EVIDENCE_TRACE_TRAFFIC_NOTE =
  "Finding evidence-trace (Core review) - FindingInspectView with finding-first H1, Back to finding CTA, PageContextualHelpButton (topic map findings; Category-1 path matcher), policy citation hero, demoted policy sectionTitle, OperatorEvidenceLimitsFooter without self-link (TB-1826-TB-1829). Absorbs former RR `/inspect` redirect hit share. Single-finding trace - not a full sealed-record diligence Sources trail alone. Score 72/100 (2026-08-08) - core finding evidence-trace at RRF Evidence band; hard-caps higher Evidence without sealed-record diligence Sources trail. Owner pass: cannot improve further toward 80 without turning this into a sealed-record diligence Sources trail.";
