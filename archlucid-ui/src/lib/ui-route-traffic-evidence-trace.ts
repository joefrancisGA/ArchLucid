/**
 * Traffic workbook row ID for finding evidence-trace (canonical inspect surface).
 * Owner backlog shorthand: ERU.
 */
export const EVIDENCE_TRACE_TRAFFIC_ROW_ID = "ERU";

/** Canonical path tracked on the ERU workbook row. */
export const EVIDENCE_TRACE_TRAFFIC_PATH =
  "/architecture/reviews/[runId]/findings/[findingId]/evidence-trace";

/** Workbook Section column value. */
export const EVIDENCE_TRACE_TRAFFIC_SECTION = "Core review";

/**
 * Owner workbook Notes for ERU - documents Evidence chrome on finding evidence-trace.
 * ASCII-only for Windows console note scripts.
 */
export const EVIDENCE_TRACE_TRAFFIC_NOTE =
  "Finding evidence-trace (Core review) - FindingInspectView with finding-first H1, Back to finding CTA, PageContextualHelpButton (topic map findings; Category-1 path matcher), EvidenceTraceEvidenceOrientationStrip (Sources + claim-discipline), policy citation hero, demoted governance sectionTitle, OperatorEvidenceLimitsFooter without self-link (TB-1826-TB-1829). Absorbs former RR `/inspect` redirect hit share. Single-finding trace - not a full signed-record diligence package alone. Does not imply CPA SOC 2 or third-party pen-test publication. Score 58/100 (2026-08-08) - post finding-first Evidence pass; hard-caps higher Evidence without full diligence packing. Owner pass: cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
