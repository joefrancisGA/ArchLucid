/**
 * Traffic workbook row ID for Recommendation learning ops.
 * Owner backlog shorthand: INR.
 */
export const RECOMMENDATION_LEARNING_TRAFFIC_ROW_ID = "INE";

/** Canonical path tracked on the INR workbook row. */
export const RECOMMENDATION_LEARNING_TRAFFIC_PATH = "/internal/recommendation-learning";

/** Workbook Section column value (owner catalog). */
export const RECOMMENDATION_LEARNING_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for INE - documents Evidence chrome on recommendation learning.
 * ASCII-only for Windows console note scripts.
 */
export const RECOMMENDATION_LEARNING_TRAFFIC_NOTE =
  "Recommendation learning (Admin) - RecommendationLearningOpsPageClient with PageContextualHelpButton (topic map pilot-feedback; Category-1 registry), workspace Sources + claim-discipline orientation strip, profile state / eligibility / preview rebuild / version history. Sibling PRC = pilot feedback; ADV = advisory scans. Internal ranking-profile ops console - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 45/100 (2026-08-08) - admin ops console hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
