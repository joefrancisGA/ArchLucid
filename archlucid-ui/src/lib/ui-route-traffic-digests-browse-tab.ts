import { DIGESTS_BROWSE_TAB_PATH } from "@/lib/digests-route-paths";

/**
 * Traffic workbook row ID for Digests Browse tab.
 * Owner backlog shorthand: ARB (template formerly DIB / ARD-as-browse).
 */
export const DIGESTS_BROWSE_TAB_TRAFFIC_ROW_ID = "ARB";

/** Canonical path tracked on the ARB workbook row. */
export const DIGESTS_BROWSE_TAB_TRAFFIC_PATH = DIGESTS_BROWSE_TAB_PATH;

/** Workbook Section column value - query-tab on Digests hub. */
export const DIGESTS_BROWSE_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for ARB - documents Evidence chrome inherited from ARD hub on Browse tab.
 * ASCII-only for Windows console note scripts.
 */
export const DIGESTS_BROWSE_TAB_TRAFFIC_NOTE =
  "Digests Browse tab (Tab surface) - inherits ARD hub Evidence chrome (DigestsHubClient DigestsPageHeader PageContextualHelp + Category-1 registry on /architecture/digests; Sources follow-up strip below tabs, no claim-boundary band). DigestsHubClient syncs ?tab=browse; mounts DigestsBrowseContent (DigestsBrowseSetupChecklist + history). Sibling ARS = schedule; AIS = subscriptions; ARD = hub; HDI = Digests help.tab-surface ceiling below parent hub Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
