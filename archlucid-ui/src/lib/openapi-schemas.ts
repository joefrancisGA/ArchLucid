/**
 * Single import surface for openapi-typescript component/path maps generated from the v1 snapshot.
 * UI domain types under `types/authority.ts` should alias schemas here rather than maintaining parallel shapes.
 */
export type { components, paths } from "./api-types.generated";
