/**
 * Traffic workbook row ID for Tenant settings.
 * Owner backlog shorthand: ATE (template formerly STX on legacy settings path).
 */
export const TENANT_SETTINGS_TRAFFIC_ROW_ID = "ATE";

/** Canonical path tracked on the ATE workbook row. */
export const TENANT_SETTINGS_TRAFFIC_PATH = "/administration/tenant";

/** Workbook Section column value (template catalog). */
export const TENANT_SETTINGS_TRAFFIC_SECTION = "Settings";

/**
 * Owner workbook Notes for ATE - documents Evidence chrome on Tenant settings.
 * ASCII-only for Windows console note scripts.
 */
export const TENANT_SETTINGS_TRAFFIC_NOTE =
  "Tenant settings (Settings/Admin) - TenantSettingsPageView with PageContextualHelpButton (topic map scope; Category-1 registry), workspace Sources + claim-discipline orientation strip, workspace defaults / quality gates / cost settings. Sibling STR = recycle-bin; DIS = digests schedule. Access configuration - not a signed-record Sources trail. Score 50/100 (2026-08-06) - settings access-hub hard-caps higher Evidence.";
