/**
 * Traffic workbook row ID for Decision register.
 * Owner backlog shorthand: GDO.
 */
export const DECISION_REGISTER_TRAFFIC_ROW_ID = "GDO";

/** Canonical path tracked on the GDO workbook row. */
export const DECISION_REGISTER_TRAFFIC_PATH = "/governance/decision-register";

/** Workbook Section column value. */
export const DECISION_REGISTER_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for GDO - documents Evidence chrome on Decision register.
 * ASCII-only for Windows console note scripts.
 */
export const DECISION_REGISTER_TRAFFIC_NOTE =
  "Decision register (Alerts/gov) - DecisionRegisterClient with OperatorPageHeader PageContextualHelpButton (Learn more omitted - no decision-register specialty; TB-2050; not governance-approval catch-all), Category-1 registry, filters/summary/cards/timeline. Not a signed-record Sources trail by itself. Score 68/100 (2026-08-08) - register browse at GFN Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
