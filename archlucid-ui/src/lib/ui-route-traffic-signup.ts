/**
 * Traffic workbook row ID for Signup.
 * Owner backlog shorthand: SIG.
 */
export const SIGNUP_TRAFFIC_ROW_ID = "SIG";

/** Canonical path tracked on the SIG workbook row. */
export const SIGNUP_TRAFFIC_PATH = "/signup";

/** Workbook Section column value. */
export const SIGNUP_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for SIG - documents Evidence chrome on Signup.
 * ASCII-only for Windows console note scripts.
 */
export const SIGNUP_TRAFFIC_NOTE =
  "Signup (Marketing) - SignupForm (TB-2010 disable-until-ready) or SignupAccessRequestForm (fields visible; zod email gate; explained disabled primary; data-use + Privacy line), SignupEvaluationAsideRail (what-happens-next steps, tenant/sign-in/no-payment posture, sample review), SignupEvidenceOrientationStrip (quiet Scope claim + Security & trust; no Related strip; no FAQ/Trust/Sign-in body dupes; wordmark text-sm vs nav text-xs). Not an operator PageContextualHelp surface. Score 58/100 (2026-08-08) - marketing evaluation-access ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
