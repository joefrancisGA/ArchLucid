/**
 * Traffic workbook row ID for operator Billing & plans settings.
 * Owner backlog shorthand: ABI (template formerly SBE on legacy settings/billing path).
 */
export const OPERATOR_BILLING_SETTINGS_TRAFFIC_ROW_ID = "ABI";

/** Canonical path tracked on the ABI workbook row. */
export const OPERATOR_BILLING_SETTINGS_TRAFFIC_PATH = "/administration/billing";

/** Workbook Section column value (template catalog). */
export const OPERATOR_BILLING_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ABI - documents Evidence chrome on Billing & plans.
 * ASCII-only for Windows console note scripts.
 */
export const OPERATOR_BILLING_SETTINGS_TRAFFIC_NOTE =
  "Billing & plans (Settings/Admin) - OperatorBillingSettingsClient with PageContextualHelpButton (topic map billing-and-plans; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, current plan / plans / usage / wallet. Sibling HBX = billing-and-plans help; P = /pricing; ADI = ai-usage. Commercial controls - not a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
