/**
 * Traffic workbook row ID for Admin tenant health.
 * Owner backlog shorthand: ATX.
 */
export const TENANT_HEALTH_TRAFFIC_ROW_ID = "ATX";

/** Canonical path tracked on the ATX workbook row. */
export const TENANT_HEALTH_TRAFFIC_PATH = "/internal/tenant-health";

/** Workbook Section column value (owner catalog). */
export const TENANT_HEALTH_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ATX - documents Evidence chrome on tenant health.
 * ASCII-only for Windows console note scripts.
 */
export const TENANT_HEALTH_TRAFFIC_NOTE =
  "Tenant health (Admin) - TenantHealthAdminPageClient with PageContextualHelpButton (topic map troubleshooting; Category-1 registry), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), engagement/governance/funnel table. Internal CS engagement scores - not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 45/100 (2026-08-08) - admin KPI hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
