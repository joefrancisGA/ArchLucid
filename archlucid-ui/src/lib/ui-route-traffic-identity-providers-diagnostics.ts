/**
 * Traffic workbook row ID for Identity providers diagnostics.
 * Owner backlog shorthand: SEI.
 */
export const IDENTITY_PROVIDERS_DIAGNOSTICS_TRAFFIC_ROW_ID = "SEI";

/** Canonical path tracked on the SEI workbook row. */
export const IDENTITY_PROVIDERS_DIAGNOSTICS_TRAFFIC_PATH =
 "/administration/identity-providers/diagnostics";

/** Workbook Section column value (template catalog). */
export const IDENTITY_PROVIDERS_DIAGNOSTICS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for SEI - documents Evidence chrome on Identity diagnostics.
 * ASCII-only for Windows console note scripts.
 */
export const IDENTITY_PROVIDERS_DIAGNOSTICS_TRAFFIC_NOTE =
  "Identity diagnostics (Settings/Admin) - IdentityProvidersDiagnosticsPageView with PageContextualHelpButton (shared header; topic map enterprise-onboarding; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, health probes / OIDC+SAML strips / token test mapping. Sibling AID = identity-providers hub; AOI = oidc; ADO = role-mapping. Access diagnostic surface - not a signed-record Sources trail.admin KPI/config ceiling below ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
