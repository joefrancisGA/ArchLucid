/**
 * Traffic workbook row ID for sponsor executive summary.
 * Owner backlog shorthand: SPE.
 */
export const EXECUTIVE_SUMMARY_TRAFFIC_ROW_ID = "SPE";

/** Canonical browser path for the sponsor executive summary report. */
export const EXECUTIVE_SUMMARY_TRAFFIC_PATH = "/insights/executive-summary";

/** Workbook Section column value - operator sponsor-report job, not pre-login marketing. */
export const EXECUTIVE_SUMMARY_TRAFFIC_SECTION = "Sponsor report";

/**
 * Owner workbook Notes for SPE - sponsor-report surface, not marketing.
 * Absorbs former VXX (legacy `/value-report` redirect) hit share.
 * ASCII-only for Windows console note scripts.
 */
export const EXECUTIVE_SUMMARY_TRAFFIC_NOTE =
  "Sponsor executive summary (Sponsor report) - ValueReportPageClient/View with PageContextualHelpButton (topic map executive-summary; Category-1 registry on /sponsor-report + /value-report), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), period preview/exports, Outcomes nav. Absorbs former VXX hit share from retired legacy `/value-report` bookmark row (LegacyValueReportRedirectPage still permanentRedirects here). Period summary - not a signed-record Sources trail alone. Score 68/100 (2026-08-08) - period summary at ARE sponsor Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
