/** IH-046 — insight density is generation demotion, not a support-band filter. */
export const INHABIT_FINDINGS_DENSITY_IS_GENERATION_SENTENCE =
  "Insight density demotes low-signal engines to checklist rows during generation. Support bands on each row are separate — hiding demoted engines is not completeness." as const;
