/**
 * Traffic workbook row ID for Role mapping settings.
 * Owner backlog shorthand: ADO (template formerly SEO on legacy settings path).
 */
export const ROLE_MAPPING_SETTINGS_TRAFFIC_ROW_ID = "ADO";

/** Canonical path tracked on the ADO workbook row. */
export const ROLE_MAPPING_SETTINGS_TRAFFIC_PATH =
  "/administration/identity-providers/role-mapping";

/** Workbook Section column value (template catalog). */
export const ROLE_MAPPING_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADO - documents Evidence chrome on Role mapping.
 * ASCII-only for Windows console note scripts.
 */
export const ROLE_MAPPING_SETTINGS_TRAFFIC_NOTE =
  "Role mapping (Settings/Admin) - IdentityProvidersRoleMappingPageView with PageContextualHelpButton (topic map users-and-roles; Category-1 registry), workspace Sources + claim-discipline orientation strip, status / examples / SAML+diagnostics CTAs. Sibling AUX = users; AID = identity-providers hub. Access configuration - not a signed-record Sources trail. Score 50/100 (2026-08-05) - settings access-hub hard-caps higher Evidence.";
