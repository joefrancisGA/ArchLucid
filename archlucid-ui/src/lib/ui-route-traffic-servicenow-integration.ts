/**
 * Traffic workbook row ID for ServiceNow integration.
 * Owner backlog shorthand: ISX.
 */
export const SERVICENOW_INTEGRATION_TRAFFIC_ROW_ID = "ISX";

/** Canonical path tracked on the ISX workbook row. */
export const SERVICENOW_INTEGRATION_TRAFFIC_PATH = "/integrations/servicenow";

/** Workbook Section column value (template catalog). */
export const SERVICENOW_INTEGRATION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for ISX - documents Evidence chrome on ServiceNow integration.
 * ASCII-only for Windows console note scripts.
 */
export const SERVICENOW_INTEGRATION_TRAFFIC_NOTE =
  "ServiceNow integration (Integrations) - ServiceNowIntegrationPageClient with PageContextualHelpButton (topic map integration-readiness; Category-1 registry), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), connection status, CMDB incident overrides. Outbound ITSM config hub - not a signed-record Sources trail. Sibling IJX = Jira; INA = Azure Boards; SCE = cloud connections. Does not imply CPA SOC 2 or third-party pen-test publication. Score 48/100 (2026-08-08) - integration-config hub hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";