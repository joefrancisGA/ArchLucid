/**
 * Traffic workbook row ID for honest `/help/api-contracts` alias.
 * Owner backlog shorthand: HEP (template formerly HAP).
 */
export const API_CONTRACTS_HELP_ALIAS_TRAFFIC_ROW_ID = "HEP";

/** Canonical path tracked on the HEP workbook row. */
export const API_CONTRACTS_HELP_ALIAS_TRAFFIC_PATH = "/help/api-contracts";

/** Workbook Section column value - slug alias under Help. */
export const API_CONTRACTS_HELP_ALIAS_TRAFFIC_SECTION = "Help alias";

/**
 * Owner workbook Notes for HEP - documents Evidence chrome inherited via HelpApiContractsGuideView.
 * ASCII-only for Windows console note scripts.
 */
export const API_CONTRACTS_HELP_ALIAS_TRAFFIC_NOTE =
 "Honest api-contracts help alias (Help alias) - slug alias api-contracts -> governance-api-contracts (TB-1386); renders HelpApiContractsGuideView Evidence chrome (Sources strip + claim-discipline + PageContextualHelp + Category-1 on alias path). Canon HG = /help/governance-api-contracts. Body lives in API_CONTRACTS.md (TB-1388 contributor strip). Template HAP renamed to HEP to match owner. Sibling HG = canon topic. Score 52/100 (2026-08-08) - help-topic orientation hard-caps higher Evidence (alias inherits HG). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
