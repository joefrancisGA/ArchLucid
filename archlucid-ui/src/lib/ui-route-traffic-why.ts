/**
 * Traffic workbook row ID for why marketing page.
 * Owner backlog shorthand: WHY.
 */
export const WHY_TRAFFIC_ROW_ID = "WHY";

/** Canonical path tracked on the WHY workbook row. */
export const WHY_TRAFFIC_PATH = "/why";

/** Workbook Section column value (template catalog). */
export const WHY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for WHY Ã¢â‚¬â€ documents Evidence chrome on why.
 */
export const WHY_TRAFFIC_NOTE =
  "Why ArchLucid (Marketing) - WhyArchlucidMarketingView with WhyEvidenceOrientationStrip (evaluation Sources + claim-discipline: marketing comparison only). Not an operator PageContextualHelp surface. Sibling WXX = /welcome; WH = /why-archlucid learning twin; SEC = /security-trust.marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
