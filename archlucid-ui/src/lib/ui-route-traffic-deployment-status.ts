/**
 * Traffic workbook row ID for Deployment status admin diagnostics.
 * Owner backlog shorthand: ADE.
 */
export const DEPLOYMENT_STATUS_TRAFFIC_ROW_ID = "ADE";

/** Canonical path tracked on the ADE workbook row. */
export const DEPLOYMENT_STATUS_TRAFFIC_PATH = "/internal/deployment-status";

/** Workbook Section column value (template catalog). */
export const DEPLOYMENT_STATUS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADE - documents Evidence chrome on Deployment status.
 * ASCII-only for Windows console note scripts.
 */
export const DEPLOYMENT_STATUS_TRAFFIC_NOTE =
  "Deployment status (Admin) - AdminDeploymentStatusPageView with PageContextualHelpButton (topic map troubleshooting; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, BUILD_ID agreement + refresh. Sibling ADY = system-health; ADD = demo-readiness; ATX = tenant-health. Internal release identity - not a signed-record Sources trail.admin KPI/config ceiling below ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
