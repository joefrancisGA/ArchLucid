/**
 * Traffic workbook row ID for Connect AWS securely help.
 * Owner backlog shorthand: HEC (owner HEW renamed to match template cloud-connections/aws HEC).
 */
export const CONNECT_AWS_SECURELY_HELP_TRAFFIC_ROW_ID = "HEC";

/** Canonical path tracked on the HEC workbook row. */
export const CONNECT_AWS_SECURELY_HELP_TRAFFIC_PATH = "/help/cloud-connections/aws";

/** Workbook Section column value - help alias under cloud-connections. */
export const CONNECT_AWS_SECURELY_HELP_TRAFFIC_SECTION = "Help alias";

/**
 * Owner workbook Notes for HEC - documents Evidence chrome on aws cloud-connections help.
 * ASCII-only for Windows console note scripts.
 */
export const CONNECT_AWS_SECURELY_HELP_TRAFFIC_NOTE =
  "Connect AWS securely help (Help alias) - HelpConnectAwsSecurelyGuideView with PageContextualHelpButton (topic map cloud-connections-aws; Category-1 registry), claim-discipline orientation strip with Sources, verification callout + Re-poll now deep links, troubleshoot section, OIDC federation identifiers + copyable trust-policy template, manifest-driven IAM permissions table, configure CTA. Sibling HCE = parent cloud-connections help; HC = Azure; HGC = GCP; INC = live AWS settings. Owner HEW renamed to HEC to match template. Orientation guide - not a signed-record Sources trail. Score 58/100 (2026-08-07) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
