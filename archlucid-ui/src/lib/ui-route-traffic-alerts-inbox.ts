/**
 * Traffic workbook row ID for Alert inbox.
 * Owner backlog shorthand: AL.
 */
export const ALERTS_INBOX_TRAFFIC_ROW_ID = "AL";

/** Canonical path tracked on the AL workbook row. */
export const ALERTS_INBOX_TRAFFIC_PATH = "/governance/alerts";

/** Workbook Section column value. */
export const ALERTS_INBOX_TRAFFIC_SECTION = "Alerts/gov";

/**
 * Owner workbook Notes for AL - documents Evidence chrome on Alert inbox.
 * ASCII-only for Windows console note scripts.
 */
export const ALERTS_INBOX_TRAFFIC_NOTE =
  "Alert inbox (Alerts/gov) - AlertsHubChrome with PageContextualHelpButton (topic map alerts; Category-1 registry), streaming inbox body. Sibling SAX = alert rules; HA = alerts help; ALE = routing tab. Not a signed-record Sources trail by itself. Score 72/100 (2026-08-08) - alert triage launcher at ARE/GFN Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
