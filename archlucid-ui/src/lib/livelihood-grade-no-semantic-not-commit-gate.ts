/** LN-033 — ADR 0085 semantic Unchecked band warns; it is not the default commit gate. */
export const LIVELIHOOD_GRADE_NO_SEMANTIC_NOT_COMMIT_GATE_LINE =
  "Semantic support band (ADR 0085) may warn on Unchecked at finalize; structural provenance (0082) remains the persist gate. LLM judge is not default-on (LN-025)." as const;
