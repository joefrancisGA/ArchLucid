import { ADVISORY_SCANS_HREF } from "@/lib/advisory-scans-route";

/**
 * Traffic workbook row ID for Advisory scans hub (default Scans tab).
 * Owner backlog shorthand: ADV.
 */
export const ADVISORY_SCANS_TRAFFIC_ROW_ID = "ADV";

/** Canonical path tracked on the ADV workbook row. */
export const ADVISORY_SCANS_TRAFFIC_PATH = ADVISORY_SCANS_HREF;

/** Workbook Section column value (template catalog). */
export const ADVISORY_SCANS_TRAFFIC_SECTION = "Advisory";

/**
 * Owner workbook Notes for ADV - documents Evidence chrome on Advisory scans.
 * ASCII-only for Windows console note scripts.
 */
export const ADVISORY_SCANS_TRAFFIC_NOTE =
  "Advisory scans hub (Governance) - AdvisoryHubClient with PageContextualHelpButton (Category-1 registry), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), Scans/Schedules tabs, recommendation generate + schedules. Follow-up recommendations - not a signed-record Sources trail. Sibling AD = Schedules tab deep link; ADT = Scans tab deep link. Does not imply CPA SOC 2 or third-party pen-test publication. Score 50/100 (2026-08-08) - recommendation-launcher hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
