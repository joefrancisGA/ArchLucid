/**
 * Traffic workbook row ID for Repeat-review loop help.
 * Owner backlog shorthand: HRX.
 */
export const REPEAT_REVIEW_LOOP_HELP_TRAFFIC_ROW_ID = "HRX";

/** Canonical path tracked on the HRX workbook row. */
export const REPEAT_REVIEW_LOOP_HELP_TRAFFIC_PATH = "/help/repeat-review-loop";

/** Workbook Section column value - in-app help topic, not marketing. */
export const REPEAT_REVIEW_LOOP_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HRX - documents Evidence chrome on repeat-review-loop help.
 * ASCII-only for Windows console note scripts.
 */
export const REPEAT_REVIEW_LOOP_HELP_TRAFFIC_NOTE =
  "Repeat-review loop help (Help topic) - HelpRepeatReviewLoopGuideView with PageContextualHelpButton (topic map repeat-review-loop; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, start-loop CTAs, prepared SECOND_RUN / REPEAT_REVIEW_LOOP markdown. Sibling CXX = compare-two-reviews; REP = /replay; COR = first-architecture-review. Score 58/100 (2026-08-05) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
