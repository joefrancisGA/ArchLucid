/**
 * Traffic workbook row ID for Sponsor Sponsor scorecard.
 * Owner backlog shorthand: ESX.
 */
export const SPONSOR_SCORECARD_TRAFFIC_ROW_ID = "ESX";

/** Canonical path tracked on the ESX workbook row. */
export const SPONSOR_SCORECARD_TRAFFIC_PATH = "/sponsor/scorecard";

/** Workbook Section column value (template catalog). */
export const SPONSOR_SCORECARD_TRAFFIC_SECTION = "Sponsor";

/**
 * Owner workbook Notes for ESX — documents Evidence chrome on Sponsor scorecard.
 */
export const SPONSOR_SCORECARD_TRAFFIC_NOTE =
 "Retired — sponsor scorecard bookmark `/sponsor/scorecard` merged onto ARE (`/architecture/sponsor-dashboard`). Legacy path hard-retired (host-gate 404); hit share absorbed by ARE.";
