/**
 * Traffic workbook row ID for Pattern library detail.
 * Owner backlog shorthand: INA (template formerly PAP).
 */
export const PATTERN_LIBRARY_DETAIL_TRAFFIC_ROW_ID = "INA";

/** Canonical workbook path tracked on the INA row (dynamic pattern key). */
export const PATTERN_LIBRARY_DETAIL_TRAFFIC_PATH = "/insights/patterns/[patternKey]";

/** Workbook Section column value (catalog; insights operator surface). */
export const PATTERN_LIBRARY_DETAIL_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for INA - documents Evidence chrome on pattern detail.
 * ASCII-only for Windows console note scripts.
 */
export const PATTERN_LIBRARY_DETAIL_TRAFFIC_NOTE =
  "Pattern library detail (Insights) - PatternLibraryDetailClient with OperatorPageHeader PageContextualHelpButton (Category-1 via /insights/patterns prefix; topic map label-only Learn more omitted TB-2050), pattern overview/risks/evidence/governance sections + use-in-review CTAs. Sibling INP = /insights/patterns hub. Anonymized catalog guidance Ã¢â‚¬â€ not a signed-record Sources trail.marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without sealed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a sealed-record diligence Sources trail.";
