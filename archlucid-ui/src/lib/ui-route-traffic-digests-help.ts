/**
 * Traffic workbook row ID for Digests help.
 * Owner backlog shorthand: HDG (owner formerly HDI).
 */
export const DIGESTS_HELP_TRAFFIC_ROW_ID = "HDG";

/** Canonical path tracked on the HDG workbook row. */
export const DIGESTS_HELP_TRAFFIC_PATH = "/help/digests";

/** Workbook Section column value (template catalog). */
export const DIGESTS_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HDG Ã¢â‚¬â€ Digests specialty help (TB-2049).
 * ASCII-only for Windows console note scripts.
 */
export const DIGESTS_HELP_TRAFFIC_NOTE =
  "Digests help (Help topic) - HelpDigestsGuideView with PageContextualHelpButton (topic map digests; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, Schedule/Subscriptions/Browse CTAs, how-digests-work stepper. Operator orientation guide - not a signed-record Sources trail. Sibling ARD = hub; ARS = schedule; ARB = browse; AIS = subscriptions. Owner HDI renamed to HDG to match template. Score 58/100 (2026-08-08) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without sealed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a sealed-record diligence Sources trail.";
