import { REVIEWS_NEW_GUIDED_INTAKE_HREF } from "@/lib/reviews-new-path-copy";

/**
 * Traffic workbook row ID for Start review Guided intake path tab.
 * Owner backlog shorthand: ENE.
 */
export const REVIEWS_NEW_GUIDED_INTAKE_TAB_TRAFFIC_ROW_ID = "ENE";

/** Canonical path tracked on the ENE workbook row. */
export const REVIEWS_NEW_GUIDED_INTAKE_TAB_TRAFFIC_PATH = REVIEWS_NEW_GUIDED_INTAKE_HREF;

/** Workbook Section column value - query-path tab on Start review hub. */
export const REVIEWS_NEW_GUIDED_INTAKE_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for ENE - documents Evidence chrome inherited from RNX hub on Guided intake.
 * ASCII-only for Windows console note scripts.
 */
export const REVIEWS_NEW_GUIDED_INTAKE_TAB_TRAFFIC_NOTE =
 "Start review Guided intake tab (Tab surface) - inherits RNX hub Evidence chrome (ReviewsNewPageChrome PageContextualHelpButton + Category-1 registry on /architecture/reviews/new; Sources strip above path tabs). ReviewsNewPathSwitcher syncs ?path=guided-intake; mounts SocraticIntakeWizard. Sibling REQ = quick-review; REN = detailed; RNX = hub. Score 48/100 (2026-08-07) - path-tab surface hard-caps higher Evidence (below RNX launcher). Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
