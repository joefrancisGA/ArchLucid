import { DIGESTS_SCHEDULE_TAB_PATH } from "@/lib/settings-admin-route-paths";

/** Retired pre-release executive digest settings bookmark (TB-1901–TB-1905). */
export const RETIRED_SETTINGS_EXEC_DIGEST_PATH = "/settings/exec-digest" as const;

/** Canonical executive digest schedule surface (traffic row DIS). */
export const CANONICAL_EXEC_DIGEST_SCHEDULE_PATH = DIGESTS_SCHEDULE_TAB_PATH;
