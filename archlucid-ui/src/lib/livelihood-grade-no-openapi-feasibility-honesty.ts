/** LN-029 — no new OpenAPI fields required; LN-004 uses existing feasibility verdict shapes. */
export const LIVELIHOOD_GRADE_NO_OPENAPI_FEASIBILITY_HONESTY_NOTE =
  "LN-029 / LN-004: uncited hard export block uses existing FeasibilityVerdict hardCitations and CareerArtifactCompletenessValidator codes — no OpenAPI snapshot change." as const;
