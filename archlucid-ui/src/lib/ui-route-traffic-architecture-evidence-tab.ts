/**
 * Traffic workbook row ID for create-home Evidence archTab.
 * Owner backlog shorthand: REE.
 */
export const ARCHITECTURE_EVIDENCE_TAB_TRAFFIC_ROW_ID = "REE";

/** Canonical path tracked on the REE workbook row. */
export const ARCHITECTURE_EVIDENCE_TAB_TRAFFIC_PATH =
  "/architecture/reviews/[runId]?archTab=evidence" as const;

/** Workbook Section column value — create-home tab surface. */
export const ARCHITECTURE_EVIDENCE_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for REE — create-home Evidence archTab Evidence chrome.
 * ASCII-only for Windows console note scripts.
 */
export const ARCHITECTURE_EVIDENCE_TAB_TRAFFIC_NOTE =
  "Create-home Evidence archTab (Tab surface) - mounts on ArchitectureCreatedWorkspace when fromGeneration+create-architecture and no signed manifest; ArchitectureCreatedWorkspace syncs ?archTab=evidence. ArchitectureCreatedEvidenceOrientationStrip (PageContextualHelpButton + Category-1 registry; Sources + claim-discipline) above RunDetailCaptureEvidenceSection / BulkEvidenceUpload (TB-1846 sibling band). Twin committed surface is reviewTab=evidence on ReviewDetailWorkspace (RRE hub chrome). Sibling REA = activity; REC = clarifications; RED = diagram; REF = findings; REG = governance; REO = overview. Capture upload only - not a signed-record Sources trail. Score 48/100 (2026-08-08) - create-home path-tab hard-caps higher Evidence (below RRE hub).";
