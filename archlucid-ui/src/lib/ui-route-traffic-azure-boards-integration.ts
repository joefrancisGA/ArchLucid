/**
 * Traffic workbook row ID for Azure Boards integration settings.
 * Owner backlog shorthand: INZ.
 */
export const AZURE_BOARDS_INTEGRATION_TRAFFIC_ROW_ID = "INZ";

/** Canonical path tracked on the INZ workbook row. */
export const AZURE_BOARDS_INTEGRATION_TRAFFIC_PATH = "/integrations/azure-boards";

/** Workbook Section column value (template catalog). */
export const AZURE_BOARDS_INTEGRATION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for INZ - documents Evidence chrome on Azure Boards settings.
 * ASCII-only for Windows console note scripts.
 */
export const AZURE_BOARDS_INTEGRATION_TRAFFIC_NOTE =
  "Azure Boards integration (Integrations) - AzureBoardsIntegrationPageClient with OperatorPageHeader PageContextualHelpButton (topic map azure-boards; Category-1 registry), connection settings + health probe + default work-item behavior. Outbound ITSM config hub — not a signed-record Sources trail. Sibling IJX = Jira; ISX = ServiceNow; HEZ = azure-boards help; ACS = connection-status. Score 48/100 (2026-08-08) - integration-config hub hard-caps higher Evidence.";
