/**
 * Traffic workbook row ID for Webhooks integration.
 * Owner backlog shorthand: IWX.
 */
export const WEBHOOKS_INTEGRATION_TRAFFIC_ROW_ID = "IWX";

/** Canonical path tracked on the IWX workbook row. */
export const WEBHOOKS_INTEGRATION_TRAFFIC_PATH = "/integrations/webhooks";

/** Workbook Section column value (template catalog). */
export const WEBHOOKS_INTEGRATION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for IWX - documents Evidence chrome on Webhooks integration.
 * ASCII-only for Windows console note scripts.
 */
export const WEBHOOKS_INTEGRATION_TRAFFIC_NOTE =
  "Webhooks integration (Integrations) - WebhooksIntegrationPageClient with PageContextualHelpButton (topic map integration-readiness; Category-1 registry), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), endpoint list + delivery history. Sibling IJX = Jira; ISX = ServiceNow; OID = DLQ; ADY = system-health. Outbound webhook config - not a signed-record Sources trail. Score 58/100 (2026-08-08) - connector config connector config ceiling below SCE Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";