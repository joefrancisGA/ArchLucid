/**
 * Traffic workbook row ID for sponsor sponsor report.
 * Owner backlog shorthand: SPE.
 */
export const SPONSOR_SUMMARY_TRAFFIC_ROW_ID = "SPE";

/** Canonical browser path for the sponsor sponsor report report. */
export const SPONSOR_SUMMARY_TRAFFIC_PATH = "/insights/sponsor-report";

/** Workbook Section column value - operator sponsor-report job, not pre-login marketing. */
export const SPONSOR_SUMMARY_TRAFFIC_SECTION = "Sponsor report";

/**
 * Owner workbook Notes for SPE - sponsor-report surface, not marketing.
 * Absorbs former VXX (legacy `/value-report` redirect) hit share.
 * ASCII-only for Windows console note scripts.
 */
export const SPONSOR_SUMMARY_TRAFFIC_NOTE =
  "Sponsor report (Sponsor report) - PilotValueReportPageClient/View with PageContextualHelpButton (topic map sponsor-report; Category-1 registry on /sponsor-report + /value-report), Learn more / claim-discipline orientation strip (Sources follow-up removed TB-2092), reporting-period narrative plus DOCX and board-pack exports, Outcomes nav. Absorbs former VXX hit share from retired legacy `/value-report` bookmark row (LegacyValueReportRedirectPage still permanentRedirects here). Period summary - not a signed-record Sources trail alone. Score 68/100 (2026-08-08) - period summary at ARE sponsor Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
