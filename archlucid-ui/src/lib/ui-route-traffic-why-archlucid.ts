/**
 * Traffic workbook row ID for operator Why ArchLucid proof page.
 * Owner backlog shorthand: WH.
 */
export const WHY_ARCHLUCID_TRAFFIC_ROW_ID = "WH";

/** Canonical path tracked on the WH workbook row. */
export const WHY_ARCHLUCID_TRAFFIC_PATH = "/why-archlucid";

/** Workbook Section column value (template catalog). */
export const WHY_ARCHLUCID_TRAFFIC_SECTION = "Learning";

/**
 * Owner workbook Notes for WH — documents Evidence chrome on why-archlucid.
 */
export const WHY_ARCHLUCID_TRAFFIC_NOTE =
  "Why ArchLucid proof (Learning) - WhyArchLucidPage with PageContextualHelpButton (topic map getting-started; Category-1 registry), workspace Sources + claim-discipline orientation strip, seeded demo telemetry + sponsor pack sections. Sibling WHY = marketing /why. Demo/proof orientation — not a signed-record Sources trail. Does not imply CPA SOC 2 or third-party pen-test publication. Score 45/100 (2026-08-05) — learning demo-proof page hard-caps higher Evidence.";
