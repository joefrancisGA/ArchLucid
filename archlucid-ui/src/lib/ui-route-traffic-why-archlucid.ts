/**
 * Traffic workbook row ID for operator Why ArchLucid proof page.
 * Owner backlog shorthand: WH.
 */
export const WHY_ARCHLUCID_TRAFFIC_ROW_ID = "WH";

/** Canonical path tracked on the WH workbook row. */
export const WHY_ARCHLUCID_TRAFFIC_PATH = "/why-archlucid";

/** Workbook Section column value (template catalog). */
export const WHY_ARCHLUCID_TRAFFIC_SECTION = "Learning";

/**
 * Owner workbook Notes for WH â€” documents Evidence chrome on why-archlucid.
 */
export const WHY_ARCHLUCID_TRAFFIC_NOTE =
 "Why ArchLucid proof (Learning) - WhyArchLucidPage with PageContextualHelpButton (topic map getting-started; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, seeded demo telemetry + sponsor pack sections. Sibling WHY = marketing /why. Demo/proof orientation â€” not a signed-record Sources trail. Score 45/100 (2026-08-05) â€” learning demo-proof page hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
