/**
 * Traffic workbook row ID for Demo readiness admin diagnostics.
 * Owner backlog shorthand: ADD.
 */
export const DEMO_READINESS_TRAFFIC_ROW_ID = "ADD";

/** Canonical path tracked on the ADD workbook row. */
export const DEMO_READINESS_TRAFFIC_PATH = "/internal/demo-readiness";

/** Workbook Section column value (template catalog). */
export const DEMO_READINESS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADD - documents Evidence chrome on Demo readiness.
 * ASCII-only for Windows console note scripts.
 */
export const DEMO_READINESS_TRAFFIC_NOTE =
  "Demo readiness (Admin) - DemoReadinessAdminPageClient with PageContextualHelpButton (topic map path-chooser; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, BuyerCtoDemoReadinessPanel internal diagnostics. Sibling ATD = trial-funnel; ATX = tenant-health; ADY = system-health. Internal employee diagnostic - not a signed-record Sources trail. Score 45/100 (2026-08-05) - admin diagnostic hub hard-caps higher Evidence (same band as ATD/ATX).";
