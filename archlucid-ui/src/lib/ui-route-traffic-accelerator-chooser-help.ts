/**
 * Traffic workbook row ID for Accelerator chooser help.
 * Owner backlog shorthand: HAX.
 */
export const ACCELERATOR_CHOOSER_HELP_TRAFFIC_ROW_ID = "HAX";

/** Canonical path tracked on the HAX workbook row. */
export const ACCELERATOR_CHOOSER_HELP_TRAFFIC_PATH = "/help/accelerator-chooser";

/** Workbook Section column value - in-app help topic, not marketing. */
export const ACCELERATOR_CHOOSER_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for HAX - documents Evidence chrome on accelerator-chooser help.
 * ASCII-only for Windows console note scripts.
 */
export const ACCELERATOR_CHOOSER_HELP_TRAFFIC_NOTE =
  "Pick a starter proof pack help (Help topic) - HelpAcceleratorChooserGuideView with PageContextualHelpButton (topic map accelerator-chooser; Category-1 registry), claim-discipline orientation strip, prerequisite panel, per-pack Start CTAs from ACCELERATOR_CHOOSER_ENTRIES (technical inputs behind disclosure), workflow steps with baseline/quick-review links. No GTM/V1.1 out-of-scope roadmap band (buyer-safe; claim discipline only). Sibling HPX = path-chooser; COR = first-architecture-review; HE. = catch-all residual. Not bare HelpTopicMarkdownView. Score 68/100 (2026-08-09) - specialty chooser chrome with Start CTAs; help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
