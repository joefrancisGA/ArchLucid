import { RETIRED_PILOT_OUTCOMES_PATH } from "@/lib/sponsor-report-navigation";

/**
 * Traffic workbook row ID for pilot outcomes under Insights.
 * Owner backlog shorthand: SPP.
 */
export const PILOT_OUTCOMES_TRAFFIC_ROW_ID = "SPP";

/** Canonical browser path for the pilot outcomes sponsor-report slice. */
export const PILOT_OUTCOMES_TRAFFIC_PATH = RETIRED_PILOT_OUTCOMES_PATH;

/** Workbook Section column value - operator sponsor-report job, not pre-login marketing. */
export const PILOT_OUTCOMES_TRAFFIC_SECTION = "Sponsor report";

/**
 * Owner workbook Notes for SPP - pilot outcomes surface, not marketing.
 * ASCII-only for Windows console note scripts.
 */
export const PILOT_OUTCOMES_TRAFFIC_NOTE =
  "Pilot outcomes (Sponsor report) - PilotValueReportPageView with PageContextualHelpButton (topic map pilot-outcomes; Category-1 registry), PilotOutcomesEvidenceOrientationStrip (workspace Sources + claim-discipline: pilot outcomes only), Outcomes nav. Sibling SPE = /insights/sponsor-report; HPO = /help/pilot-outcomes. Period pilot outcomes - not a signed-record Sources trail alone. Score 68/100 (2026-08-08) - pilot outcomes launcher at SPE Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a sealed-record diligence Sources trail.";
