/** CE-019 — help: how to sketch a labeled change on the architecture desk. */
export const CHEAP_EXPLORATION_HELP_SKETCH_A_CHANGE_SLUG = "sketch-a-change" as const;

export const CHEAP_EXPLORATION_HELP_SKETCH_A_CHANGE_TITLE = "Sketch a change" as const;

export const CHEAP_EXPLORATION_HELP_SKETCH_A_CHANGE_OVERVIEW =
  "After spawn lock, use Sketch a change on the architecture desk to clone from the sealed snapshot into a new editable draft. The sketch stays Rehearsal-stamped (ADR 0092) until you explicitly execute a Record review — it is not Career proof and cannot Compare as sealed-record evidence until both sides are committed manifests." as const;
