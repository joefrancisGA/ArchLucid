import { POLICY_PACK_DELTA_DEMO_HELP_PATH } from "@/lib/policy-pack-delta-demo-help-route";

/**
 * Traffic workbook row ID for Policy-pack delta demo help.
 * Owner backlog shorthand: POL.
 */
export const POLICY_PACK_DELTA_DEMO_HELP_TRAFFIC_ROW_ID = "POL";

/** Canonical path tracked on the POL workbook row. */
export const POLICY_PACK_DELTA_DEMO_HELP_TRAFFIC_PATH = POLICY_PACK_DELTA_DEMO_HELP_PATH;

/** Workbook Section column value Ã¢â‚¬â€ in-app help topic, not marketing. */
export const POLICY_PACK_DELTA_DEMO_HELP_TRAFFIC_SECTION = "Help topic";

/**
 * Owner workbook Notes for POL Ã¢â‚¬â€ documents the specialty SE/Admin demo guide surface.
 * ASCII-only for Windows console note scripts.
 */
export const POLICY_PACK_DELTA_DEMO_HELP_TRAFFIC_NOTE =
  "Specialty policy-pack delta demo (internal runbook) - HelpPolicyPackDeltaDemoGuideView with policy-packs / standards-and-rules / audit primary CTAs, Sources diligence strip (findings, audit-trail help, governance-approval, alerts), 5-minute narrative arc, claim-discipline callout (dry-run is not certification), PageContextualHelp, and prepared POLICY_PACK_DELTA_DEMO_SCRIPT.md (TB-1727 leakage strip). Not bare HelpTopicMarkdownView. Admin-gated internal Help Center tier. Score 58/100 (2026-08-08) - help-topic ceiling below HEL hub band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
