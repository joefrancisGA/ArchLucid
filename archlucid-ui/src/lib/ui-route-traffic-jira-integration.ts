/**
 * Traffic workbook row ID for Jira integration.
 * Owner backlog shorthand: IJX.
 */
export const JIRA_INTEGRATION_TRAFFIC_ROW_ID = "IJX";

/** Canonical path tracked on the IJX workbook row. */
export const JIRA_INTEGRATION_TRAFFIC_PATH = "/integrations/jira";

/** Workbook Section column value (template catalog). */
export const JIRA_INTEGRATION_TRAFFIC_SECTION = "Integrations";

/**
 * Owner workbook Notes for IJX — documents Evidence chrome on Jira integration.
 */
export const JIRA_INTEGRATION_TRAFFIC_NOTE =
  "Jira integration (Integrations) - ItsmProductIntegrationPageClient (product=jira) with PageContextualHelpButton (topic map integration-readiness; Category-1 registry), workspace Sources + claim-discipline orientation strip, health probe, tenant outbound overrides. Outbound ITSM config hub — not a signed-record Sources trail. Sibling ISX = ServiceNow; INA = Azure Boards; SCE = cloud connections. Score 48/100 (2026-08-04) — integration-config hub hard-caps higher Evidence.";
