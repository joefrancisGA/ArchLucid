/**
 * Traffic workbook row ID for Improvement plan detail.
 * Owner backlog shorthand: INL (template formerly PPP).
 */
export const PLANNING_PLAN_DETAIL_TRAFFIC_ROW_ID = "INL";

/** Canonical path tracked on the INL workbook row. */
export const PLANNING_PLAN_DETAIL_TRAFFIC_PATH = "/insights/improvement-planning/plans/[planId]";

/** Workbook Section column value. */
export const PLANNING_PLAN_DETAIL_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for INL - documents Evidence chrome on plan detail.
 * ASCII-only for Windows console note scripts.
 */
export const PLANNING_PLAN_DETAIL_TRAFFIC_NOTE =
  "Improvement plan detail (Insights) - PlanningPlanDetailPageView with OperatorPageHeader PageContextualHelpButton (Learn more omitted inherits planning hub; TB-2050; Category-1 registry for /insights/improvement-planning/plans), plan sections. Derived plan Ã¢â‚¬â€ not a signed-record Sources trail. Sibling hub = /insights/improvement-planning.marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
