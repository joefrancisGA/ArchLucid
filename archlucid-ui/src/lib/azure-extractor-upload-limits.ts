/**
 * Upper bound for Azure extractor ZIP uploads — keep aligned with
 * {@link ArchLucid.Application.AzureExtractor.AzureExtractorUploadLimits.MaxZipBytes} on the server.
 */
export const ARCH_LUCID_AZURE_EXTRACTOR_MAX_ZIP_BYTES: number = 52 * 1024 * 1024;
