/**
 * Traffic workbook row ID for legacy /demo entry redirect.
 * Owner backlog shorthand: DXX.
 */
export const DEMO_ENTRY_TRAFFIC_ROW_ID = "DXX";

/** Canonical path tracked on the DXX workbook row. */
export const DEMO_ENTRY_TRAFFIC_PATH = "/demo";

/** Workbook Section column value. */
export const DEMO_ENTRY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for DXX — documents Evidence chrome on the /demo redirect shim.
 * ASCII-only for Windows console note scripts.
 */
export const DEMO_ENTRY_TRAFFIC_NOTE =
  "Demo entry redirect (Marketing) - DemoEntryRedirect client shim with DemoEntryEvidenceOrientationStrip (evaluation Sources + claim-discipline: interim redirect to CTO demo tour). Not an operator PageContextualHelp surface. Sibling DPX = /demo/preview; DEX = /demo/explain. marketing/learning ceiling below operator Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
