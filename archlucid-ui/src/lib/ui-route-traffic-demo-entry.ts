/**
 * Traffic workbook row ID for shareable demo entry.
 * Owner backlog shorthand: DXX.
 */
export const DEMO_ENTRY_TRAFFIC_ROW_ID = "DXX";

/** Canonical path tracked on the DXX workbook row. */
export const DEMO_ENTRY_TRAFFIC_PATH = "/demo";

/** Workbook Section column value (template catalog). */
export const DEMO_ENTRY_TRAFFIC_SECTION = "Marketing";

/**
 * Owner workbook Notes for DXX — documents Evidence chrome on demo entry redirect.
 */
export const DEMO_ENTRY_TRAFFIC_NOTE =
  "Demo entry (Marketing) - DemoEntryRedirect client shim with interim and Continue link while redirecting to CTO tour or home. Shareable entry redirect — not a signed-record Sources trail. Sibling DPX = /demo/preview. Score 28/100 (2026-08-04) — redirect/shim hard-caps higher Evidence.";
