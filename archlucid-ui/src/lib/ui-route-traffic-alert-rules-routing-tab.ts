import { ALERT_ROUTING_TAB_PATH } from "@/lib/alert-routing-evidence-copy";

/**
 * Traffic workbook row ID for Alert rules Notifications (routing) tab.
 * Owner backlog shorthand: ALE (template catalog may still label the same path GOR).
 */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_ROW_ID = "ALE";

/** Canonical path tracked on the ALE workbook row. */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_PATH = ALERT_ROUTING_TAB_PATH;

/** Workbook Section column value. */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for ALE - documents Evidence chrome on the Notifications tab.
 * ASCII-only for Windows console note scripts.
 */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_NOTE =
 "Alert rules Notifications/routing tab (Tab surface) - AlertRoutingContent with hub PageContextualHelpButton (topic map alerts), Learn more / claim-discipline orientation strip when ?tab=routing (Sources follow-up removed TB-2092). Delivery destinations only - not diligence Sources. Sibling SAX/GOA = hub; Conditions/Advanced/Test tabs are separate rows. Score 48/100 (2026-08-08) - routing-tab surface hard-caps higher Evidence. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
