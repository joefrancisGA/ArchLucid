import { ALERT_ROUTING_TAB_PATH } from "@/lib/alert-routing-evidence-copy";

/**
 * Traffic workbook row ID for Alert rules Notifications (routing) tab.
 * Owner backlog shorthand: GOR (template catalog; ALE was a prior alias for the same path).
 */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_ROW_ID = "GOR";

/** Canonical path tracked on the GOR workbook row. */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_PATH = ALERT_ROUTING_TAB_PATH;

/** Workbook Section column value. */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_SECTION = "Tab surface";

/**
 * Owner workbook Notes for GOR — documents Evidence chrome on the Notifications tab.
 */
export const ALERT_RULES_ROUTING_TAB_TRAFFIC_NOTE =
  "Alert rules Notifications/routing tab (Tab surface) - AlertRoutingContent with hub PageContextualHelpButton (topic map alerts), workspace Sources + claim-discipline orientation strip when ?tab=routing. Delivery destinations only — not diligence Sources. Sibling SAX/GOA = hub; Conditions/Advanced/Test tabs are separate rows. Does not imply CPA SOC 2 or third-party pen-test publication.";
