/**
 * Traffic workbook row ID for API keys settings.
 * Owner backlog shorthand: ADP (template formerly SAE on legacy settings/api-keys path).
 */
export const API_KEYS_SETTINGS_TRAFFIC_ROW_ID = "ADP";

/** Canonical path tracked on the ADP workbook row. */
export const API_KEYS_SETTINGS_TRAFFIC_PATH = "/administration/api-keys";

/** Workbook Section column value (template catalog). */
export const API_KEYS_SETTINGS_TRAFFIC_SECTION = "Admin";

/**
 * Owner workbook Notes for ADP - documents Evidence chrome on API keys.
 * ASCII-only for Windows console note scripts.
 */
export const API_KEYS_SETTINGS_TRAFFIC_NOTE =
  "API keys (Admin/Settings) - ApiKeysSettingsPageClient with PageContextualHelpButton (topic map users-and-roles; Category-1 registry), Learn more / claim-discipline (Sources follow-up removed TB-2092) orientation strip, summary / credentials / recent events. Sibling AUX = users; HCX = cli-usage help. Automation credential controls - not a signed-record Sources trail.admin hub at SET/ADY Evidence band; hard-caps higher Evidence without signed-record diligence Sources trail. Owner pass: Evidence chrome shipped; cannot improve further toward 80 without turning this into a signed-record diligence Sources trail.";
