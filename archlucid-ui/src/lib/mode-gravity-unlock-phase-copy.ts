/** MG-020 — operate-unlock vocabulary is eval-shaped; Working must not see progressive-hide copy. */
export const MODE_GRAVITY_UNLOCK_PHASE_COPY_MODULE =
  "first-pilot-operate-unlock-vocabulary" as const;

export const MODE_GRAVITY_UNLOCK_EVAL_ONLY_LINE =
  "Operate unlock progressive vocabulary is eval-shaped — gate behind eval chrome on Working (ADR 0094)." as const;
