/**
 * @deprecated Horizontal nav replaced by `SidebarNav` + `MobileNavDrawer`. Re-exported for tests and bookmarks.
 * Documentation search lives in `HelpSearchPanel` (header icon / Shift+/); guides dialog remains `HelpPanel`.
 */
export { SidebarNav as ShellNav } from "@/components/SidebarNav";
