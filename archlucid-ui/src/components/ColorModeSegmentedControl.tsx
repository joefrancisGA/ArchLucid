"use client";

import { ThemePreferenceSelector } from "@/components/ThemePreferenceSelector";

/** @deprecated Use ThemePreferenceSelector on the Preferences page. */
export function ColorModeSegmentedControl() {
  return <ThemePreferenceSelector />;
}
