/**
 * Canonical L0 MUST question keys for live E2E (mirrors `UniversalIntakeQuestions.cs`).
 * Domain identifiers — not API secrets; listed in `.gitleaks.toml` allowlist.
 */
export const L0_ACTOR_ADDITIONAL_KINDS_QUESTION_KEY = "l0.actor.additional-kinds";
