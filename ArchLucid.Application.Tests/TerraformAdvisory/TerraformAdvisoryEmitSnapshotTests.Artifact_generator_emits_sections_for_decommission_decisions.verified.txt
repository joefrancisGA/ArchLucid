# ArchLucid advisory – review before apply
# See ADVISORY.md in the Terraform advisory export bundle — ArchLucid never runs apply or removal on your behalf.
# Decision d1 — agent asked to change infrastructure; emitting comment-only advisory (no automated removal blocks).
# Terraform address hint: (unspecified — validate against extractor manifest)

# ArchLucid advisory – review before apply
# See ADVISORY.md in the Terraform advisory export bundle — ArchLucid never runs apply or removal on your behalf.
# Decision d2 — agent asked to change infrastructure; emitting comment-only advisory (no automated removal blocks).
# Terraform address hint: (unspecified — validate against extractor manifest)
