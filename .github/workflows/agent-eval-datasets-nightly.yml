# Nightly: offline agent eval dataset + prompt-injection fixture validation (same gates as CI Tier 1 subset).
# Extends Tier 1 with a scheduled signal; does not invoke live LLMs (see scripts/ci/eval_agent_quality.py header).

name: Agent eval datasets nightly

on:
  workflow_dispatch:
  schedule:
    - cron: "15 4 * * *"

permissions:
  contents: read

jobs:
  eval-datasets:
    if: github.event.repository.fork == false
    name: "eval_agent_quality (manifest + prompt-injection)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Validate eval manifest
        run: python3 scripts/ci/eval_agent_quality.py --manifest-only --strict

      - name: Prompt-injection regression fixtures
        run: python3 scripts/ci/eval_agent_quality.py --prompt-injection-only --strict

      - name: Eval corpus (recall + simulator AgentResult quality)
        run: python3 scripts/ci/eval_agent_corpus.py --markdown-report "${{ runner.temp }}/eval-corpus-nightly.md"

      - name: Retrieval IR golden harness (recall@5 + MRR)
        run: python3 scripts/ci/eval_retrieval_ir.py --report "${{ runner.temp }}/retrieval-ir-report.md"
