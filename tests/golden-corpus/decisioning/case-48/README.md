# case-48

Pinned Azure inventory lists an unattached managed disk with no matching topology node on the graph. Expect **orphaned-azure-resource** (also **azure-inventory-reconciliation** on empty graph vs inventory).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
