# case-51

Pinned AWS inventory lists an unattached EBS volume with no matching topology node on the graph. Expect **orphaned-aws-resource** (also **aws-inventory-reconciliation** on empty graph vs inventory).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
