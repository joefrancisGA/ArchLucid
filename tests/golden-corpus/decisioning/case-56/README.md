# case-56

Pinned AWS inventory ZIP includes `advisor-cost.json` with one cost recommendation row and an empty `resources.json`. Expect **aws-cost-recommendation** (also **topology-coverage** on empty graph).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
