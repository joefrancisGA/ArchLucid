# case-55

Pinned Azure inventory ZIP includes `advisor-cost.json` with one Advisor recommendation and an empty `resources.json`. Expect **advisor-cost-recommendation** (also **topology-coverage** on empty graph).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
