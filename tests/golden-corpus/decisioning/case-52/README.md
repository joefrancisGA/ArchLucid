# case-52

Pinned GCP inventory lists an unattached persistent disk with no matching topology node on the graph. Expect **orphaned-gcp-resource** (also **gcp-inventory-reconciliation** on empty graph vs inventory).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
