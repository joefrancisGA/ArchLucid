# case-57

Pinned GCP inventory ZIP includes `recommender-cost.json` with one Recommender cost projection row and an empty `resources.json`. Expect **gcp-cost-recommendation** (also **topology-coverage** on empty graph).

Regenerated with `ARCHLUCID_RECORD_DECISIONING_GOLDEN=1`.
