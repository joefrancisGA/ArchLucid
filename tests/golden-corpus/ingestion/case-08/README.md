# case-08

Bicep storage account with `publicNetworkAccess: 'Enabled'` in the resource `properties` block. Expects compacted `tf.publicnetworkaccess` plus ARM alias `publicNetworkAccess`.

Deterministic parser: `BicepInfrastructureDeclarationParser` (no LLM).
