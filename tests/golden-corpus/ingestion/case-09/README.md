# case-09

Kubernetes JSON list with a privileged `Deployment` and a `LoadBalancer` `Service`. Expects `k8s.privileged` and `k8s.servicetype` projection.

Deterministic parser: `KubernetesJsonInfrastructureDeclarationParser` (no LLM).
