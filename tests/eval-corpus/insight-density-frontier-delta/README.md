# Insight-density frontier-delta fixtures

Hand-authored regression scenarios for `InsightDensityFrontierDeltaCalculator` and `scripts/ci/insight_density_frontier_delta.py`.

These are **not** captured frontier-model transcripts. Baseline labels like `baseline-a` are neutral fixture identifiers only.
