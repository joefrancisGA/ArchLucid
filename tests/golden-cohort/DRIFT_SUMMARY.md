> **Scope:** Human-readable rollup of simulator-vs-real golden cohort drift — regenerated by drift tests when reports exist.

# Simulator vs real LLM drift summary

This file is overwritten when **`SimulatorVsRealDriftTests`** completes successfully with drift JSON written under `tests/golden-cohort/drift-reports/`.

If you see only this placeholder, run the manual workflow **golden-cohort-simulator-real-drift** (or execute the test locally with `ARCHLUCID_REAL_AOAI_TEST_*` set).

## Latest averages

_(No drift reports found on last generation.)_

## Highest divergence scenarios

_(None.)_

## Trend

Append-only machine-readable events live in `tests/golden-cohort/drift-trend.jsonl` when generated (may be gitignored locally).
