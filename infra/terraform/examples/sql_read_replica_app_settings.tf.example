# Example App Service settings for optional read replica routing (authority run lists).
# Wire SqlServer:ReadReplica:AuthorityRunListReadsConnectionString to a secondary SQL endpoint
# (Azure SQL read scale-out, geo-replica, or route-separated connection string).
# Do not expose SMB/file shares for SQL connectivity; use private endpoints per environment policy.

# locals {
#   primary_sql    = azurerm_mssql_server.main.fully_qualified_domain_name
#   replica_sql    = azurerm_mssql_server.replica.fully_qualified_domain_name
#   sql_admin_user = var.sql_admin_login
#   sql_admin_pass = var.sql_admin_password
# }

# resource "azurerm_linux_web_app" "api" {
#   ...
#   app_settings = {
#     "ConnectionStrings__ArchiForge" =
#       "Server=tcp:${local.primary_sql},1433;Database=ArchiForge;User ID=${local.sql_admin_user};Password=${local.sql_admin_pass};Encrypt=True;TrustServerCertificate=False;"
#     "SqlServer__ReadReplica__AuthorityRunListReadsConnectionString" =
#       "Server=tcp:${local.replica_sql},1433;Database=ArchiForge;User ID=${local.sql_admin_user};Password=${local.sql_admin_pass};Encrypt=True;TrustServerCertificate=False;"
#   }
# }
