# Optional: add `check` / `precondition` blocks once concrete `module` sources are wired here.
# Run `terraform init` then `terraform validate` from this directory (providers are not vendored in-repo).
