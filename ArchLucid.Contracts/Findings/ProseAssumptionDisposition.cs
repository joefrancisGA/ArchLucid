namespace ArchLucid.Contracts.Findings;

/// <summary>Closed disposition for a grounded prose assumption register row (DX-61).</summary>
public enum ProseAssumptionDisposition
{
    Contradicted,
    Consistent,
    NotVerifiable,
}
