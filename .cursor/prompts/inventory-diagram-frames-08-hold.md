# IDF-HOLD — No nested frames, no global gap bump, no IDR boxes

**This prompt is not implementation.** Paste only if a session starts nested VNet/subnet/subscription cluster rects, raises `ComponentHorizontalGap` / `ComponentVerticalGap` or Mermaid `nodeSpacing` to fake frame gutters, draws RG boxes from an **IDR** chat, restores `stroke-dasharray="5 4"` / peering `6 4` on frames after IDF-03, ships Microsoft Azure product icons, defaults Graphviz `dot`, or merges disconnected components solely so one RG is a single island.

Follow [`.cursor/prompts/inventory-diagram-frames-00-index.md`](inventory-diagram-frames-00-index.md) global constraints.

## Goal

Keep IDF as **visible, honest IDA-08 frames** on the existing inventory-forest spine. Space comes from **cell chrome**. Stroke is **solid 2 px `#64748b`**. Same RG name on two islands stays **two frames**.

## Why

IDR-HOLD exists because frames on the connected-component grid overlapped. IDA-08 packed then framed; `ResolveFrameBounds` undid that by name-union. Nested Azure boxes (subscription → RG → VNet → subnet) are a second layout engine (**IDA-HOLD**). IDT/IDG forbids another dagre gap pass. Peering already owns dashed `6 4`; declared (IDP) owns `4 3`.

## Do not implement (ever from IDF sessions)

| Temptation | Hold |
|-----------|------|
| Nested VNet / subnet / NIC / subscription frames | Recursive cluster packer; **IDA-HOLD** |
| Merge disconnected components so a split RG is one box | Hides the component grid IDH/IDG kept compact |
| Draw RG rects from an **IDR** chat | **IDR-HOLD** — captions only in IDR |
| Bump `ComponentHorizontalGap` / `ComponentVerticalGap` | Dense `viewBoxToUnion` ratchet; use IDF-04 chrome |
| Another `nodeSpacing` / `rankSpacing` / `padding` pass | **IDG** hold |
| Restore frame `5 4` or reuse peering `6 4` / declared `4 3` | Containers are solid after IDF-03 |
| Teal frame stroke | Interactive affordance, not grouping |
| Undo `FlattenSparseSubgraphs` so Mermaid “shows RGs” | Empty-box regression |
| Restore `alpack_*` | Mermaid 11 LR flip |
| Live canvas Graphviz `dot` / elk / React Flow inventory | Forest stays live; `fdp` is PNG/fallback |
| Microsoft Azure product icons | License; **IDA-HOLD** |
| Desktop review tabs behind **More** | workspace rule |
| GTM M-90 / M-44 / M-91 / M-92; TB-135 / TB-136 | Owner/GTM |

## If a session is already implementing a hold item

Stop. Revert uncommitted hold-item code. Point at **IDF-01–IDF-07**. For nested frames, write a one-paragraph owner question — do not start a packer.

## Done when

The hold is written. No code from this file.
