# LK-15 — Working mock CI identity is the operator desk

**Do not fork SD-05** if `playwright.operator-mock.config.ts` is already the default mock identity for blocking CI — then close residuals (help/palette first-session, leftover buyer-polish as the unnamed default). **Do not delete** the buyer golden-path suite. This file changes the **training identity**: default mock E2E that contributors run must boot **Working / operator desk**, not buyer-polish first-session.

## Goal

The mock config that CI and local `npm` mock scripts run without extra flags uses Working chrome (`resolveProductionDeskChrome` / Working default). Buyer-polish / Guided first-session remains an explicit suite or grep tag. Help and palette leftovers that still list destinations Working nav hides are out of this prompt if SD-11 owns them — only fix call sites this file must touch to stop the default mock asserting two peer start products (ADR 0069).

## Why

The team’s muscle memory is the product. If mock CI trains first-run success and two start doors, overlays on Working will keep regressing. A livelihood desk cannot be the optional personality.

## Context

- `playwright.operator-mock.config.ts`
- Buyer-polished Playwright / `NEXT_PUBLIC_OPERATOR_EXPERIENCE`
- `workspace-mode.ts` Working default
- ADR 0069 vs ADR 0067 tests (`create-review-peer-parity.test.tsx`)
- SD-06 help first-session leftover — do not rewrite all help; do not fork SD-06 unless a mock assertion requires one topic

## What to build

1. Default mock E2E config/docs/scripts: Working operator identity.
2. Keep buyer golden-path as a named suite.
3. Fail a focused test if default mock env enables buyer-polish without an explicit flag.
4. Do not auto-switch stored Guided users in product code.

## Acceptance criteria

- A contributor running the default mock path sees Working one-primary chrome, not two peer start products.
- Guided/demo suites still exist and pass when explicitly selected.

## Constraints

- Do not collapse review tabs.
- Do not implement GTM cohorts.
- Do not change the density gate.
