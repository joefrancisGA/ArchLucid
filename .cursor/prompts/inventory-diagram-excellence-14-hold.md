# IDX-HOLD — No complete-graph, no default NIC/PE cards, no Microsoft icons, no Full-subscription RBAC

**This prompt is not implementation.** Paste only if a session starts a complete graph to “maximize edges,” shows NIC or private-endpoint cards by default, ships Microsoft Azure product icons, dumps RBAC `HAS_ROLE` / `MAY_ACCESS` onto Full subscription, calls Azure at diagram compile time, re-runs **AX-DE / IDA / IDF / IE-RF** as greenfield, or starts nested VNet frames from an **IDA / IDF** chat instead of **IDX-05 / IDX-06**.

Follow [`.cursor/prompts/inventory-diagram-excellence-00-index.md`](inventory-diagram-excellence-00-index.md) global constraints.

## Goal

Keep IDX as **honest excellence** on the existing inventory-forest + one-collector spine. More edges only when cited. Nested Azure boxes only from IDX-05/06. PE cards only from IDX-10 opt-in. The hold applies to **every** `DiagramMode`: Executive, Architecture, Network, Security, Identity, Data, DataFlow, DataArchitecture, FullSubscription, ResourceGroup, SelectedResources, DependencyNeighborhood.

## Why

Owner asked to maximize edges, then asked for excellence ideas. A complete graph inside each RG will raise the walkthrough `{e}` count and make the diagram worse. Microsoft icons are a license decision (**IDA-HOLD**). Full-subscription RBAC is a hairball, not architecture. Nested frames from IDA/IDF chats skip the shared packer and break Identity/DataFlow.

## Do not implement (ever from IDX sessions)

| Temptation | Hold |
|-----------|------|
| Complete graph / all-to-all same-RG mesh to raise edge counts | **IDX-03** cited hops; collocation last-resort only |
| NIC cards on the canvas | NeverShow; compose hops instead |
| Private-endpoint cards default on | **IDX-10** opt-in; hops still used |
| Microsoft Azure product icons / CDN `IMG` | License; **IDA-HOLD** |
| RBAC / `MAY_ACCESS` dump on Full subscription / Executive | Identity / Security / DataFlow overlays only |
| Azure HTTP at `DiagramAst` compile / render | Snapshots are append-only |
| `Export-AzResourceGroup` / ARM `dependsOn` arrows | **IE-RF-12** |
| Second Azure collector / GET every id | Plane; type-scoped lists only |
| Nested VNet/subnet/subscription frames from **IDA** or **IDF** paste files | Those holds still apply **in those waves**; nesting is **IDX-05 / IDX-06** |
| Replace DataFlow stages or DataArchitecture type groups with RG-first packing | Mode contract |
| Merge disconnected components so one RG is a single island | Honest split frames |
| Raise `ComponentHorizontalGap` / Mermaid `nodeSpacing` | IDF-04 chrome / IDG hold |
| Graphviz `dot` as live canvas | `fdp` PNG; forest live |
| Desktop review tabs behind **More** | workspace rule |
| GTM M-90 / M-44 / M-91 / M-92; TB-135 / TB-136 | Owner/GTM |
| Observed traffic / flow logs as architecture arrows | SN-RT / SN-PE, not IDX |

## If a session is already implementing a hold item

Stop. Revert uncommitted hold-item code. Point at **IDX-01–IDX-13**. For icons, write a one-paragraph owner question (license source, dark-mode, PNG) — do not download SVGs. For “maximize edges no matter what,” point at IDX-01 recapture + IDX-02 compose + IDX-03 cited hops.

## Done when

The hold is written. No code from this file.
