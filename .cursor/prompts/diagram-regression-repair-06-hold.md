# DRR-HOLD — Written hold (not implementation)

**Wave:** diagram regression repair (**DRR**). **Not an implementation prompt.** Paste only when a session starts to restore the subscription box, vendor a new icon zip, or re-run an older diagram wave as greenfield.

## Goal

Keep the repair inside the #3586 / #3587 regressions. Do not reopen layout waves that already shipped, and do not undo the owner’s request to drop the subscription bounding box.

## Do not implement from DRR sessions

| Temptation | Hold |
|-----------|------|
| Restore `subscription-frame` / `cluster_subscription` | Owner asked to remove that box. `ShouldDraw` stays false |
| Draw resource-group frames across data-flow stage columns | RG stays a card caption on the data-flow path. Inventory RG frames stay on the existing packer |
| Put Executive, Network, or Full subscription on `LayoutDataFlowStageColumns` | Stage columns are `(DataFlow)` titles only |
| Draw edges so every node looks connected | No synthetic edges for orphans |
| Download the Azure Architecture Center SVG zip or any icon mirror | A later owner-supplied pack. This set only stops false icons |
| Crop, recolor, or non-uniformly scale icon pixels | `xMidYMid meet` stays |
| Use a Microsoft icon as the ArchLucid mark | Owner icon rule |
| Re-run **IDA**, **IDF**, **IDX**, **IDR**, **SN-DF**, **AX-DE** as greenfield | Those waves have their own prompts |
| Retune Mermaid `nodeSpacing` / `rankSpacing` / `padding` | Not the defect |
| Switch the live canvas from inventory-forest to Graphviz `dot` | Forest stays the canvas when `layoutSvg` is present |
| Rewrite Home hub copy (“All seven destinations”, infrastructure eyebrow) | Different page. Not DRR |
| Collapse desktop review workspace tabs | Workspace rule |
| GTM **M-90 / M-44 / M-91 / M-92**; reopen **TB-135 / TB-136** | Owner/GTM |
| LLM-authored Mermaid as the diagram of record | DAU / ADR 0101 |

## Authorized slice (DRR-01–05)

```text
Data-flow stage titles + gutters + sky lane
        → skip-edges in the gutter and sky lane
Light #111827 / dark #e2e8f0 edge and arrowhead
        → icon kind, duplicate-byte drop, CDN ≠ Front Door
              → outline counts before the 200 cap, sort inside each section
```

## When to start a new prompt set instead

- The owner supplies the official Azure Architecture Center SVG zip and asks to map ARM types onto those files.
- The owner asks for a new inventory container to replace the subscription frame.
- The owner asks to change Home hub grouping or the “seven destinations” sentence.
