---
description: ArchiForge → ArchLucid rename — maintenance mode (initiative complete).
alwaysApply: true
---

# ArchLucid rename (complete)

The product rename from **ArchiForge** to **ArchLucid** was tracked in `docs/ARCHLUCID_RENAME_CHECKLIST.md`. **The initiative closed 2026-04-19** (GitHub repo `joefrancisGA/ArchLucid`, greenfield Terraform, docs; **7.8** local folder path **waived**).

## What to do each session now

1. **Do not** open the checklist looking for mandatory rename batches unless the user explicitly asks for rename follow-up.
2. If a change **reintroduces** forbidden legacy tokens, **CI** will fail — fix the change, do not add silent `ArchiForge*` config or `ARCHIFORGE_*` env fallbacks without product approval.
3. Keep the build green.

## Guardrails (unchanged)

- **Never modify historical SQL migration files** (001–028). Only update the master DDL script (`ArchLucid.sql`) and add new migrations if needed.
- **Do not reintroduce** silent `ArchiForge*` configuration or `ARCHIFORGE_*` env fallbacks without an explicit product decision; use startup warnings if legacy presence must be surfaced.
- **Terraform:** `infra/**/*.tf` must not contain the substring `archiforge` (CI guard). Brownfield state migration is documented only in `docs/archive/TERRAFORM_STATE_MV_PHASE_7_5_2026_04.md`.
- **SQL RLS object names** in the database may retain historical `Archiforge*` identifiers until a separate, explicit migration — see `docs/security/MULTI_TENANT_RLS.md`.
