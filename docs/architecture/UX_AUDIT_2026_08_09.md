> **Scope:** Owner UX score overrides after the 2026-08-08 bulk pass.
> Rubric: `docs/library/UI_UX_SCORING_RUBRIC.md` (when present).
> Scores land in `.local/owner/ui_route_traffic_estimates.md` (gitignored); rationale lives here.

# UX audit overrides — 2026-08-09

## SCX — Architecture scorecard · UX **80** (was 72)

| Key | Sub | Notes |
|-----|-----|-------|
| **H** | 17 | Savings hero + primary KPI tiles read before operational metrics; Outcomes hub tab state is clear |
| **V** | 15 | Neutral surfaces; claim-discipline strip; no pastel hero cards |
| **I** | 17 | ROI assumptions use disable-until-valid + inline readiness; deep-linked KPI focus from query |
| **A** | 16 | Keyboard reachability on Outcomes nav and assumption fields |
| **L** | 15 | Directional ROI / not financial reporting; sample-mode honesty preserved |

**Why not 72:** the bulk 2026-08-08 class pass scored SCX at 72 before the scorecard UX polish
shipped in the Scorecard UX + master push pass: live preview,
deep-linked KPI tiles, form validation on ROI assumptions, and Outcomes-hub navigation. That pass
briefly wrote `68,80`, then the batch re-score restored `68,72`. This row reinstates **80**.

**Workbook command:**

```powershell
python .\scripts\ci\set-archlucid-ui-route-score.py SCX 80 --dimension ux
```

**Do not downgrade** in a class-level batch re-score without re-reading `PilotScorecardPageView.tsx`.

## ADY — Administration System health · UX **84** (was 73)

| Key | Sub | Notes |
|-----|-----|-------|
| **H** | 18 | Exceptions hoisted into a Needs attention block above the all-green majority; readiness groups collapsed from six accordions into one table; duplicate Overall status tile removed |
| **V** | 16 | Neutral surfaces retained; status stays on chips; tile grid uses `auto-fit` so the final row has no ragged trailing cell |
| **I** | 17 | Tiles and hero carry same-page jump links (anchor semantics, focus follows scroll); freshness sits with the Refresh control it describes |
| **A** | 17 | Per-row "Technical details" disclosures have unique accessible names; severity icon makes status survive grayscale at the 11px badge scale |
| **L** | 16 | Healthy headline is qualified when a dependency is degraded or unconfigured; empty probe subsets report "Not reported" instead of inheriting a healthy default |

**Why not 73:** the 2026-08-08 pass scored the page before the `/al-ui-rate` P0 backlog closed. The
screenshot critique found the hero asserting "All required services are healthy / No blocking issues
detected" while a Distributed cache (Redis) card on the same screen read "Not registered", twenty
repetitions of "Healthy", five tiles whose only content was a status word, and the sole route to the
Diagnostics dashboard buried in a collapsed disclosure named "Technical details".

**Why not higher:** there is still no time axis — the readiness payload carries no last-transition or
status history, the page does not poll, and per-check latency is surfaced only as a per-group
slowest probe. Closing those needs an API change, not a UI change.

**Workbook command:**

```powershell
python .\scripts\ci\set-archlucid-ui-route-score.py ADY 84 --dimension ux
```

**Do not downgrade** in a class-level batch re-score without re-reading `SystemHealthPageView.tsx`
and `HealthDashboardSections.tsx`.
