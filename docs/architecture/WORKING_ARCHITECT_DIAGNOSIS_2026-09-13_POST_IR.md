> **Scope:** Fresh **working-architect / founding-contract** diagnosis against repo state **after** inhabit remain **IR-001–IR-018**. This is **not** the V1 release-readiness scorecard (`.cursor/prompts/assessment.md`). **Not wave 35.** Do not reopen closed **IH** / **IR** inventory rows as a class.
>
> **Origin:** ArchLucid is a **working-architect tool** — all-day use; livelihoods may depend on the sealed record (`docs/ARCHLUCID_FOUNDATIONAL_DESIGN_DEBATE.md` R4 / R13). ADR **0100** (Accepted): after spawn, Working inhabits the architecture; the afternoon document is architecture-nested findings.
>
> **Prior close:** [`INHABIT_REMAIN_ACCEPTANCE_2026-09-13.md`](INHABIT_REMAIN_ACCEPTANCE_2026-09-13.md) · Wave 34: [`INHABIT_ACCEPTANCE_2026-09-13.md`](INHABIT_ACCEPTANCE_2026-09-13.md) · Inventories: [`INHABIT_LEAK_INVENTORIES.md`](INHABIT_LEAK_INVENTORIES.md)
>
> **Post-IR close:** [`INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md`](INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md) — IP-002–IP-010 shipped; IP-011 partial (first-paint row open); IP-012 skip documented.

**Status (historical diagnosis):** This file named post-IR leftovers before the **IP-002–IP-011** pack shipped. For current truth, use [`INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md`](INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md) and [`INHABIT_POST_IR_LEAK_INVENTORIES.md`](INHABIT_POST_IR_LEAK_INVENTORIES.md). **Wave 35 is not started.** **G-REAL-06 is not shipped.**

# Working-architect diagnosis — after IR close (2026-09-13)

## Executive summary

**Yes — the product is still a working-architect tool.** Wave 34 + IR-001–IR-014 made the **primary** inhabit loop honest: Home continue-last, unfinished rail, pins, spawn handoff, Present, draft Room, inspect-from-inhabited-list, and spawn-lock Compare land on architecture-nested findings when `architectureId` is known.

A fresh trace (not the closed inventory booleans) still finds **incomplete inhabit on secondary surfaces**. The architect can inhabit the document on Monday morning and still be **exiled to the job inspector** by Reviews-hub Continue, global search, share, Room on review-detail, and “Open finding” on review-detail cards.

| Metric | Count |
|--------|------:|
| **Closed (do not re-run)** | Wave 34 + IR-001–IR-014 product; IR-015 peer-queue skip; IR-016–018 ratchet/close |
| **Named leftover clusters (this diagnosis)** | **5** (dual-place landings, Room on review-detail, inspect false confidence, inspector finalize, inhabited first-paint / banner) |
| **Intentional skips** | G-REAL-06, draft-diff Compare, presence/chat, unseal, merge kernels, lengthen 300s, desktop tab **More**, peer queue as Home |

**Headline:** Inhabit is the **landing preference** of the primary loop. It is not yet the **only** Working work surface. Remaining livelihood failures are **href and chrome leaks on the inspector**, not “this is still a first-session evaluator.”

## Method

1. Re-read R4 (projector MUST loop + mandatory transparency trail) and R13 (seat for a repeat professional; reasoned **no** is the product).
2. Re-read ADR **0100**: nested review-detail stays a **job inspector**; afternoon document is `/architecture/architectures/{id}/findings?runId=`.
3. Trace live Working href builders and chrome **after** IR close — do not treat inventory `true` rows as proof that every call site is closed.
4. Rank leftovers by livelihood: **false confidence** and **exiling the afternoon document** first; ceremony last.
5. Do **not** start wave 35. Do **not** paste an 80-prompt pack.

## What already shipped (do not reopen)

| Overlay | Evidence |
|---------|----------|
| Working default + inhabit ADR 0100 | `DEFAULT_WORKSPACE_MODE = "working"` · ADR 0100 Accepted |
| Nested findings landing (primary loop) | `resolveWorkingInhabitedFindingsLandingHref` on continue-last, pins, unfinished rail, spawn handoff |
| Record + Simulator start honesty (primary) | Draft footer, identity desk, wizard, Do-this-next strip |
| Quiet engines on desk + Ready | Inhabited chrome, pre-finalize, `RunProgressTracker` (IR-001) |
| Simulator never green Supported on inhabited cards / peer table | `presentDecisionGradeSemanticSupportBand` + host mode on rows |
| Dual-spine teaching | `core-pilot-steps.ts`, first-review guide, unfinished rail copy |
| Inspect from inhabited list | `governanceFindingInspectHref` + `focusedFinding` when options wired |
| Presenter + draft Room | Redirect to nested findings `roomElicitation=1` when architecture known |
| Spawn-lock committed Compare | `ArchitectureDraftHandoffPanel` |
| Reasoned NO as document | `RunDetailInfeasibleDecisionLead` on inhabited chrome + Overview; export-not-finalize |
| Finalize from findings document | `CommitRunButton` + `parentArchitectureId` → desk `highlightReviewId` |

## Remaining livelihood failures

### 1. Dual-place landings — Working + known architecture still opens the job

**Livelihood class:** R13 seat / ADR 0100 inhabit. The architect should resume **this system’s findings**, not operate **this review**.

| Surface | Evidence | Why livelihood |
|---------|----------|----------------|
| Reviews hub Continue | `resolveReviewsHubContinueReviewCandidate` calls `buildUnfinishedWorkRailItems` **without** `workingMode: true`; `RunsPageView.tsx` uses that href | Continue on `/architecture/reviews` opens nested/peer **review-detail** |
| Global search run | `use-global-search-bar.ts` `navigateToRun` → `resolveWorkingRunReviewLocator().href` | Search resume is the job inspector |
| Global search finding | `navigateToFinding` → `{reviewHref}/findings/{findingId}` | Inspect route, not nested `focusedFinding` |
| Share / copy review link | `workingShareHref` → `architectureNestedReviewPath` | Clipboard is the inspector URL (AO-09 locator, not afternoon document) |
| Reviews hub row href | `resolveReviewsHubReviewHref` / `resolveArchitectureReviewHref` | Hub table primary click is the job |
| Quick-decision “Open finding” / “Record disposition” | `QuickDecisionWorkspacePrimaryFindingCard.tsx` uses `getFindingDetailHref` / `getFindingGovernanceDispositionHref` | Cards sit on review-detail findings tab; CTAs ignore `architectureId` |
| Completion toast | `use-review-completion-notification.ts` → review-detail | Toast deep-link is the job |

**Not leaks:** spawn-handoff “View review job” (explicit inspector); in-flight `?reviewTab=activity` while analysis runs; Guided/demo peer URLs; peer `/governance/findings` (**IR-015**).

### 2. Room / projector — Room on review-detail is still in-place

**Livelihood class:** R4. The seatholder drives MUST yes / no / another on the projector. Presenter **Present** already redirects to nested findings. **Room** does not.

| Surface | Evidence |
|---------|----------|
| `ReviewRoomHeaderButton` | Toggles `roomElicitation=1` on **current** review-detail pathname; no `parentArchitectureId` |
| `RunDetailPresenterElicitationBridge` | Redirect `useEffect` is **presenter-only**; `roomElicitation=1` mounts `ReviewRoomElicitationPanel` inline on the inspector |
| Alt+M | `ReviewRoomElicitationShortcutHost` clicks the visible Room control — same leak |

Post-spawn MUST on nested findings (`InhabitedFindingsRoomCard`) and draft-desk Room (when `parentArchitectureId` known) stay closed.

### 3. False confidence — inspect chip and secondary re-run

**Livelihood class:** R4 trail / R13 sealed-record. Simulator work must not scan as Career-complete.

| Surface | Evidence |
|---------|----------|
| Finding inspect header / body / inspect section | `FindingDetailHeader.tsx`, `FindingDetailInspectBody.tsx`, `FindingSemanticSupportBandInspectSection.tsx` mount `FindingSemanticSupportBandChip` **without** `structuralExecutionMode` → wire `Supported` can render green **Supported** on Simulator |
| Progress-tracker terminal-failure re-run | `RunProgressTracker.tsx` `ReRunReviewButton` without `WorkingExecuteStartHonestyNotices` |
| Quality-warnings re-run | `RunAgentQualityWarningsPanel.tsx` same |
| Package-strip finalize | `ReviewPackageDoThisNextStrip.tsx` `CommitRunButton` **omits** `parentArchitectureId` (success stays on inspector — cluster 4) |

Primary start-honesty surfaces and inhabited-row support chips remain closed.

### 4. Inspector finalize does not return to the desk

**Livelihood class:** ADR 0100 — finalize is a verb that **returns to the desk**.

| Surface | `parentArchitectureId` | Success |
|---------|------------------------|---------|
| Inhabited findings finalize | Yes | Architecture desk + highlight |
| Review-package Do this next finalize | **No** | Close / stay on inspector |
| Other inspector `CommitRunButton` mounts that omit the prop | Same footgun | Same |

Canonical findings-document finalize stays closed.

### 5. Inhabited first-paint and job-scope banner

**Livelihood class:** R13 reasoned **no** + dual-spine teaching. Lower rank than 1–4.

| Surface | Evidence |
|---------|----------|
| Nested findings infeasible + trail | `InhabitedFindingsDocumentChrome` loads verdict/trail via client `useQuery` — reasoned **no** is post-hydration |
| Run-scope banner | `GovernanceFindingsQueueScopeSection` still shows “Showing findings for review … **Open review**” even when inhabited chrome is mounted (`scopedRunId` branch is **not** gated on `suppressPipelineChrome`) |

## Intentional skips (do not “fix”)

| Skip | Why |
|------|-----|
| G-REAL-06 / host Mode Real | Honesty, not a default-day lie. Host `AgentExecution:Mode` stays **Simulator**. |
| Draft-diff Compare | ADR 0092 / IH-077 |
| Presence / finding chat / unseal / 40th engine / merge kernels | ADR 0090 / 0039 / 0068 / 0070 |
| Lengthen `MUTATION_UNDO_WINDOW_SECONDS = 300` | Amend on the row after five minutes |
| Desktop review-tab **More** | Product direction |
| Peer `/governance/findings` as architecture Home | **IR-015** — run-scoped peer queue |
| Nested review-detail as **inspector** | ADR 0098 — keep the full tab strip; do not make it Monday morning |

## Constraints audit

| Constraint | Status |
|-----------|--------|
| No desktop review-tab **More** | **Pass** |
| No `typed-engine-protected` change | **Pass** (this diagnosis does not propose one) |
| No 300s undo lengthening | **Pass** |
| No GTM M-90 / M-44 / M-91 / M-92 | **Pass** |
| No TB-135 / TB-136 reopen | **Pass** |
| No G-REAL-06 | **Pass** |
| No wave 35 pack | **Pass** |

## Recommendation

1. **Do not** paste wave 35 or re-run IH-001–080 / IR-001–018 product bodies.
2. Post-IR pack close: [`INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md`](INHABIT_POST_IR_ACCEPTANCE_2026-09-13.md). Prompt index: [`.cursor/prompts/inhabit-post-00-index.md`](../../.cursor/prompts/inhabit-post-00-index.md) (**IP-001–IP-015**).
3. Keep `inhabit-leak-inventories.ts` as the ratchet for **closed** remain rows. Post-IR truth: `inhabit-post-ir-leak-inventories.ts` — do not flip closed IR/IH booleans back.

Canonical leak truth for remain-closed rows remains `archlucid-ui/src/lib/inhabit-leak-inventories.ts`. Post-IR inventory: `archlucid-ui/src/lib/inhabit-post-ir-leak-inventories.ts`.
