flowchart LR
  subgraph emit["Emit"]
    API["Api / Worker OTel"]
    SPAN["Spans authority.*<br/>archlucid.stage.name"]
    MET["Meters / gauges<br/>outbox depth timeouts"]
    LOG["Serilog + correlation id"]
  end

  subgraph pipe["Pipelines"]
    OTLP["OTLP exporters config-driven"]
    PROM["/metrics Prometheus scrape"]
  end

  subgraph consume["Consume"]
    LAW["Log Analytics / App Insights"]
    GRAF["Grafana / alert rules"]
    OPS["Support: correlation id<br/>+ runId + stage name"]
  end

  API --> SPAN --> OTLP --> LAW
  API --> MET --> PROM --> GRAF
  API --> LOG --> LAW
  LAW --> OPS
  GRAF --> OPS
