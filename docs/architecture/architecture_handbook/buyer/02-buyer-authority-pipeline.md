# 2. Authority pipeline (buyer)

A review request persists a run, then runs (or queues) five stages—context ingestion, knowledge graph, findings, decisioning, artifact synthesis—before transactional finalize into a golden manifest and decision trace.

![Authority pipeline](../../architecture_diagrams/archlucid-authority-pipeline.svg)
