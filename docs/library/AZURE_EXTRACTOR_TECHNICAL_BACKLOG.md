> **Scope:** Contributor-reference — Engineering backlog for the customer Azure extractor ZIP path, Terraform export, and citations — extends `docs/library/V1_SCOPE.md` sections 2.16–2.17; not procurement copy.

# Azure extractor and Terraform export — technical backlog

This file tracks **remaining** work after the initial ingest API, MVP PowerShell collector, CLI `aztfexport` wrapper, citation helper, and buyer-facing trust/runbook text.

## Shipped in this iteration (baseline)

- **ARC-AMPE alignment:** one Azure collector family only — customer PowerShell (`Get-ArchLucidAzurePackage.ps1`) and hosted Tier-2 reader share schema v2 ZIP layout; no second parallel collector or ARM write path.

- `POST /v1/azure-extractor/upload` with `manifest.json` schema **1–2** enforcement (v2 adds completeness metadata and optional sibling inventory files), **52 MiB** ZIP cap, optional `runId` association, SQL persistence (`dbo.AzureExtractorPackages`), audit events (`AzureExtractorPackage.*`).
- `scripts/azure/Get-ArchLucidAzurePackage.ps1`: **read-only** `Get-AzResource` inventory to `manifest.json`, `resources.json`, optional v2 sibling JSON arrays, `README.txt` (cost/advisor/retail flags warn-only until extended).
- `archlucid azure terraform-export`: non-interactive **resource-group** mode wrapping `aztfexport`, adds `ADVISORY.md`, zips output.
- `AzureExtractorCitationFormatter` for future evidence-bundle lines.
- Advisory Terraform comment templates in `ArchLucid.Application`.
- **IE-05 reconstruction:** `AdvisoryTerraformRepresentationService` builds labeled reconstruction artifacts from `AzureInventorySnapshot` rows (additive to C2 snippet path and CLI aztfexport primary export).
- **Customer-owned scheduled agent:** `Invoke-ArchLucidScheduledAzureExtractor.ps1` + Terraform Azure Automation runbook (and Function timer sample) so production collection does not require a CLI or UI pull.

## Backlog (prioritized)

1. **Extractor parity:** cost actual/amortized, Advisor cost recommendations, orphan candidates, optional Retail Prices API append; expand resource inventory (NICs, public IPs, private endpoints) with the same read-only discipline; script signing recipe plus CI URL allow-list check.
2. **Ingest hardening:** offload very large ZIPs to private blob (SQL row keeps pointer and manifest copy); download API for operators; malware scanning stance in runbook.
3. **Evidence bundle wiring:** reference stored `packageId` and manifest timestamp on generated cost lines automatically.
4. **Terraform C2:** advisory `.tf` snippets from findings with snapshot tests and `terraform fmt` / `terraform validate` CI (container job); explicit UI gate before any future `destroy`-shaped emit.
5. **Policy test:** repository-wide regression ensuring no `terraform apply` or customer-directed `terraform destroy` orchestration beyond documented third-party tool behavior.
6. **Continuous Tier 2 (ArchLucid-hosted auto-pull):** **Shipped (2026-06-21):** `AzureExtractorAutoPullHostedService` runs leader-elected polling via WIF (`WorkloadIdentityHostedAzureExtractorCredentialFactory` → `HostedAzureExtractorClient` → ingest). Gated by `AzureExtractor:AutoPull:Enabled` (default `false`) and `HostedAzureExtractor:Enabled`. **Follow-on:** ArchLucid-hosted Cost Management merge on the GET-only hosted path remains deferred — Tier 1 PowerShell is still the full-fidelity collector. Architecture: [V1_DEFERRED.md §6p](V1_DEFERRED.md); operator context: [AZURE_EXTRACTOR.md](AZURE_EXTRACTOR.md).
7. **Relationship-first topology (IE-RF):** Catalog-driven `network-associations.json` via `AzureInventoryRelationshipAssociationTypes` (Core) + ARG typed relationship projections + hosted type-scoped ARM **list** GETs (VM→NIC, all IP configs, NSG/UDR/peering, AGW/LB/Private DNS/App Service subnet). Catalog and prompts: [`INFRA_EVIDENCE_RELATIONSHIP_FIRST_COMPOSER_PROMPTS.md`](../architecture/INFRA_EVIDENCE_RELATIONSHIP_FIRST_COMPOSER_PROMPTS.md). **Do not** add `Export-AzResourceGroup` or treat ARM `dependsOn` as architecture. Hold: [`INFRA_EVIDENCE_RELATIONSHIP_FIRST_HOLD.md`](INFRA_EVIDENCE_RELATIONSHIP_FIRST_HOLD.md).
8. **Diagram enrichment collection (AX-DE):** **Done (engineering)** — AX-DE-01–18 shipped on `cursor/ax-de-implementation-80a4` (association catalog through Tier 1 optional `-IncludeAppSettingsHosts`, Databricks catalogs + VNet/subnet enrichment, Service Connector linkers, hosted GET-only honesty for app settings). Hosted stays GET-only; `config/list` is Tier 1 optional via `-IncludeAppSettingsHosts`. Catalog and prompts: [`AZURE_EXTRACTOR_DIAGRAM_ENRICHMENT_COMPOSER_PROMPTS.md`](../architecture/AZURE_EXTRACTOR_DIAGRAM_ENRICHMENT_COMPOSER_PROMPTS.md). Hold: [`AZURE_EXTRACTOR_DIAGRAM_ENRICHMENT_HOLD.md`](AZURE_EXTRACTOR_DIAGRAM_ENRICHMENT_HOLD.md). **Do not re-run.**
9. **Diagram consumption (AX-DC):** **Prompts only** — AX-DC-01–08: docs close-out, authorization endpoint inclusion on Executive/Identity/Data, Probable/Inferred confidence strokes (coordinate with IDP), completeness warnings in workbench, edge inspector, Executive **May access** e2e, hosted vs Tier 1 honesty. Catalog and prompts: [`AZURE_EXTRACTOR_DIAGRAM_CONSUMPTION_COMPOSER_PROMPTS.md`](../architecture/AZURE_EXTRACTOR_DIAGRAM_CONSUMPTION_COMPOSER_PROMPTS.md). Hold: [`AZURE_EXTRACTOR_DIAGRAM_CONSUMPTION_HOLD.md`](AZURE_EXTRACTOR_DIAGRAM_CONSUMPTION_HOLD.md).
10. **SecureNow Data Flow probable-evidence families (SN-PE):** **Prompts only** — SN-PE-01–07: family catalog (no percents), Application/messaging stages, Data Flow allow-list + endpoints, `peReachableTarget` after DNS join, Service Bus/Event Hub Sender/Receiver, honesty bands, mermaid/AST contract. Design: [`../securenow/EVIDENCE_BASED_PROBABLE_DATA_FLOWS.md`](../securenow/EVIDENCE_BASED_PROBABLE_DATA_FLOWS.md). Prompts: [`SECURENOW_PROBABLE_EVIDENCE_DATA_FLOW_COMPOSER_PROMPTS.md`](../architecture/SECURENOW_PROBABLE_EVIDENCE_DATA_FLOW_COMPOSER_PROMPTS.md). Hold: [`SECURENOW_PROBABLE_EVIDENCE_HOLD.md`](SECURENOW_PROBABLE_EVIDENCE_HOLD.md). **Do not** re-run AX-DE collection.
11. **SecureNow runtime declared / observed Data Flow (SN-RT):** **Prompts only** — SN-RT-01–10 + 12–13: Container Apps env GET into `app-settings-hosts.json`, catalog/HTTPS parse, SQL database grain + ingress FQDN, OpenAI/Search/CS hosts+stages, Queue/OpenAI/Search/ACS RBAC allowlist, opt-in LAW `dependency-observations.json` + `ObservedRuntime` family, opt-in `sys.database_principals` names, uploaded appsettings/`.env`/compose parsers + operator confirm (`HumanAssertion`), inference questionnaire for `{0}` / unresolved / same-CAE UI→API. Design: [`../securenow/RUNTIME_DECLARED_AND_OBSERVED_DATA_FLOWS.md`](../securenow/RUNTIME_DECLARED_AND_OBSERVED_DATA_FLOWS.md). Prompts: [`SECURENOW_RUNTIME_CONNECTION_COMPOSER_PROMPTS.md`](../architecture/SECURENOW_RUNTIME_CONNECTION_COMPOSER_PROMPTS.md). Hold: [`SECURENOW_RUNTIME_CONNECTION_HOLD.md`](SECURENOW_RUNTIME_CONNECTION_HOLD.md). **Do not** persist secret values. **Do not** merge observed arrows into **May access**. Hosted stays GET-only. **Do not** auto-answer the questionnaire. **Do not** re-run AX-DE / SN-DF / SN-PE as greenfield.
