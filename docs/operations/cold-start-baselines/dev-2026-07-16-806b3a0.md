# Cold-start baseline — dev (representative)

**Captured:** 2026-07-17 (from CD logs, run completed 2026-07-16 UTC)  
**Environment:** dev (GitHub Actions `target=dev`)  
**CD run:** [29542895350](https://github.com/joefrancisGA/ArchLucid/actions/runs/29542895350)  
**Commit:** `806b3a001fe243c451a62eb665f8b373d2155871`  
**Method:** [`COLD_START_MEASUREMENT.md`](../../runbooks/COLD_START_MEASUREMENT.md) procedure 1B + deployment-evidence artifact

## CD configuration (observed)

| Variable | Value |
|----------|-------|
| `CD_POST_DEPLOY_MAX_ATTEMPTS` | 6 (workflow) |
| `CD_POST_DEPLOY_RETRY_WAIT_SECONDS` | 10 |
| `CD_CANARY_ENABLED` | true |
| `CD_CANARY_BAKE_MINUTES` | 3 (sleep before smoke job) |
| `SMOKE_SYNTHETIC_PATH` | not verified in log excerpt; use `/api/auth/me` when set per **TB-758** |

## Measurements

| Phase | Value | Notes |
|-------|-------|-------|
| **A — Revision → ready** (CD wall) | ~**66 s** platform (update ~23:54:46 → canary bake start ~23:55:52 UTC) | Excludes intentional **3 min** canary bake before smoke |
| **A — With bake delay** | ~**3 min 6 s** deploy job (update start → bake end) | Bake is operator-configured wait, not ACA boot |
| **C — deployment-evidence** | **1** of **6** attempts; probe bundle **~4 s** (23:58:11 → 23:58:15 UTC) | `/health/live`, `/health/ready`, OpenAPI, `/version` passed on first bundle |
| **B — `/api/auth/me`** | *Not extracted from artifact* | Re-run local `curl -w time_total` after next dev CD or download artifact `deployment-evidence-dev-29542895350` |

## Verdict (evidence gate)

| Question | Answer |
|----------|--------|
| Free levers sufficient for this deploy? | **Yes** — evidence passed on first probe attempt; no retry budget consumed |
| Dominant bucket | **A** platform boot + migrations within ~66 s; CD gate also subject to **3 min** canary bake |
| Paid lever recommended? | **No** on this snapshot |

## Follow-up

- Add **staging** row after the next routine staging CD (same template).
- If Phase **B** is needed for a paid-lever decision, capture median `curl` timing for `/api/auth/me` post-ready.
