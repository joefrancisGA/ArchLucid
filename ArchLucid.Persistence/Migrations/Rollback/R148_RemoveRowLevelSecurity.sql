/*
  R148 rollback: intentionally empty.

  Recreating SECURITY POLICY rls.ArchLucidTenantScope and schema-bound predicates requires the full
  predicate inventory from migrations 030–129; restore from backup or replay a restored journal if needed.
*/

SET NOCOUNT ON;
GO
