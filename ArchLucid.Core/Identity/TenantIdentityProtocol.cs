namespace ArchLucid.Core.Identity;

/// <summary>Supported enterprise SSO protocols for the tenant identity provider wizard.</summary>
public enum TenantIdentityProtocol
{
    Oidc = 0,
    Saml = 1
}
