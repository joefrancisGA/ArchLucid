namespace ArchLucid.Core.InfraEvidence;

/// <summary>Ordinal Defender secure-score band for buyer-facing metadata — never a percentage.</summary>
public enum DefenderSecureScoreOrdinalBand
{
    Unknown = 0,
    Low = 1,
    Medium = 2,
    High = 3,
}
