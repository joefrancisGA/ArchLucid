namespace ArchLucid.Core.InfraEvidence;

/// <summary>Lifecycle status for a human-declared resource connection.</summary>
public enum SecurityDeclaredConnectionStatus
{
    Active = 0,
    Expired = 1,
    Revoked = 2,
}
