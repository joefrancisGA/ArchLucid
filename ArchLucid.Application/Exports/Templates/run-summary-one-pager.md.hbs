# Sponsor review summary — {{SystemName}}

**Review ID:** `{{RunId}}`
{{#if IsDemoTenant}}

> **Demo notice:** Demo tenant — replace before publishing to executives.
{{/if}}
{{#if ActiveTrialExportNotice}}

> **Trial notice:** {{ActiveTrialExportNotice}}
{{/if}}

## Deterministic findings (sealed)

{{#if HasSealedSnapshot}}
Sealed snapshot `{{FindingsSnapshotId}}` — {{SealedFindingCount}} finding(s) on the authority pipeline stream. Finalize gate, ITSM `FindingId`, and golden corpus use this record.
{{else}}
No sealed findings snapshot on this run yet — deterministic engines have not produced a sealed record.
{{/if}}

## Agent findings (advisory)

{{#if SimulatorRehearsalTitle}}

> **{{SimulatorRehearsalTitle}}** {{SimulatorRehearsalBody}}
{{/if}}

### Findings by severity

| Severity | Count |
|----------|------:|
| Critical | {{CriticalCount}} |
| High | {{HighCount}} |
| Medium | {{MediumCount}} |
| Low | {{LowCount}} |

### Sponsor report

{{SponsorReport}}

### Top findings

{{#each TopFindingTitles}}
- {{this}}
{{/each}}
{{#if CareerExportHonestyPlainText}}

## Career export coverage honesty

{{CareerExportHonestyPlainText}}
{{/if}}

---

_This Sponsor report was generated with AI assistance from agent-stream findings and severity counts. Review before sharing with sponsors._
