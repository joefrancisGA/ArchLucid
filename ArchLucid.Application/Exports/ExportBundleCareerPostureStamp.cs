namespace ArchLucid.Application.Exports;

/// <summary>CG-027 — career posture overlay stamped on CLI proof-packet bundle manifests.</summary>
public sealed record ExportBundleCareerPostureStamp(
    string StructuralExecutionMode,
    string WorkingCareerRehearsalDoor,
    bool RehearsalIncomplete,
    string CareerPosture);
