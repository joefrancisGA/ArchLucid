namespace ArchLucid.Application.Exports;

/// <summary>Simulator rehearsal copy for Markdown/DOCX exports (sense aligned with UI <c>SIMULATOR_MODE_*</c> strings).</summary>
public static class SimulatorModeExportRehearsalMarkdown
{
    public const string NoticeTitle = "Simulator mode — not live AI output";

    public const string NoticeBody =
        "This result was produced by rule-based analysis in simulator mode. Treat findings and rewrites as rehearsal only until you switch to Real mode.";
}
