namespace ArchLucid.Application.Common;

/// <summary>Prefix for canonical JWT-backed actor keys produced at the host boundary.</summary>
public static class ActorContextKeys
{
    public const string JwtActorKeyPrefix = "jwt:";
}
