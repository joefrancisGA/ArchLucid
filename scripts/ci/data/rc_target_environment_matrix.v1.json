{
  "schema": "archlucid.rc-target-environment-matrix.v1",
  "effectiveUtc": "2026-06-07T00:00:00Z",
  "ownerDecision": "Release owner 2026-06-07 — staging is contract-authoritative for RC and sponsor proof live evidence; default staging auth mode is Bearer JWT.",
  "environments": [
    {
      "id": "staging",
      "label": "Staging",
      "role": "contract-authoritative",
      "usedFor": [
        "RC drill (scripts/v1-rc-drill.ps1)",
        "Live API probes in release-readiness bundle (-ApiBaseUrl)",
        "capture-staging-readiness-evidence.ps1",
        "deployment-evidence and doctor/support-bundle against deployed API",
        "Sponsor proof packet live environment.json attachment"
      ],
      "defaultAuthMode": "Bearer",
      "authMode": "Bearer JWT (Authorization: Bearer) by default; supply via -BearerToken or ARCHLUCID_BEARER_TOKEN. ApiKey is an operator override when deployment requires it. Record the mode used in run notes and staging-readiness artifact.",
      "credentialEnvVars": [
        "ARCHLUCID_BEARER_TOKEN"
      ],
      "baseUrlSource": "GitHub staging environment secret / operator runbook — not committed to git",
      "notes": "Primary environment for buyer-facing RC signoff live evidence."
    },
    {
      "id": "production-like-local",
      "label": "Production-like (repo-local)",
      "role": "supplementary",
      "usedFor": [
        "config lint profile production-like-hosted-pilot",
        "Emit-ReleaseReadinessEvidence.ps1 offline bundle (default -Environment Production appsettings label)",
        "Terraform drift preflight and validate-config on repo host"
      ],
      "authMode": "N/A — no live API required",
      "baseUrlSource": "N/A",
      "notes": "Config and IaC posture only; does not substitute for staging live probes."
    },
    {
      "id": "production",
      "label": "Production",
      "role": "post-rc-promotion",
      "usedFor": [
        "Post-signoff promotion target",
        "Post-release smoke when explicitly scheduled"
      ],
      "authMode": "Production Entra/API key per tenant policy",
      "baseUrlSource": "Operator production runbook — not used as primary RC evidence source",
      "notes": "Production evidence may supplement staging but is not contract-authoritative for RC go/no-go unless owner explicitly overrides with written rationale."
    }
  ],
  "signoffRule": "RC go/no-go and sponsor proof live evidence must be collected from Staging unless release owner documents an approved override.",
  "docOwners": [
    "docs/library/RC_TARGET_ENVIRONMENT_MATRIX.md",
    "docs/library/V1_RELEASE_CHECKLIST.md"
  ]
}
